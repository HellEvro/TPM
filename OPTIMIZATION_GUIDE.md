# Руководство по оптимизации производительности

## 🚀 Созданные модули оптимизации

### 1. `bot_engine/async_storage.py` - Асинхронное хранилище

**Что делает:**
- Асинхронная запись файлов через ThreadPoolExecutor
- Батчинг операций записи (накопление и запись пакетами)
- Автоматический flush через таймаут или размер батча
- Очередь операций для уменьшения количества I/O операций

**Преимущества:**
- ⚡ Уменьшение блокировок основного потока
- ⚡ Батчинг уменьшает количество операций записи на диск
- ⚡ Параллельная запись нескольких файлов

**Ожидаемое улучшение:** 30-50% ускорение операций сохранения

---

### 2. `bot_engine/optimized_exchange_client.py` - Оптимизированный клиент биржи

**Что делает:**
- Connection pooling с переиспользованием соединений
- Асинхронные запросы через aiohttp
- Батчинг запросов для параллельной обработки
- Кэширование ответов (TTL 5 секунд)
- Rate limiting для защиты от блокировок

**Преимущества:**
- ⚡ Переиспользование TCP соединений (меньше overhead)
- ⚡ Параллельные запросы к бирже
- ⚡ Кэширование уменьшает количество запросов
- ⚡ Rate limiting защищает от блокировок

**Ожидаемое улучшение:** 40-60% ускорение сетевых запросов

---

### 3. `bot_engine/optimized_calculations.py` - Оптимизированные расчеты

**Что делает:**
- Пакетные расчеты RSI/EMA для множества монет
- Векторные операции через NumPy (если доступен)
- Параллельные расчеты через ThreadPoolExecutor
- Батчинг операций для лучшей производительности

**Преимущества:**
- ⚡ Векторные операции NumPy быстрее циклов Python
- ⚡ Параллельные расчеты используют все ядра CPU
- ⚡ Батчинг уменьшает overhead от создания потоков

**Ожидаемое улучшение:** 20-40% ускорение расчетов (с NumPy до 3-5x)

---

### 4. `bot_engine/performance_optimizer.py` - Интеграционный модуль

**Что делает:**
- Объединяет все оптимизации в единый интерфейс
- Автоматический fallback на стандартные функции
- Сбор статистики использования
- Управление жизненным циклом оптимизаций

---

## 📊 Ожидаемые улучшения производительности

### Сетевые запросы к бирже:

| Операция | До | После | Улучшение |
|----------|-----|-------|-----------|
| Загрузка 583 монет (RSI) | ~2-3 мин | ~1-1.5 мин | **40-50%** |
| Параллельные запросы | 50 потоков | 100 соединений | **2x пропускная способность** |
| Кэширование | Нет | 5 сек TTL | **~30% меньше запросов** |

### I/O операции:

| Операция | До | После | Улучшение |
|----------|-----|-------|-----------|
| Сохранение состояния | Синхронно | Асинхронно + батчинг | **30-50%** |
| Блокировка потока | Да | Нет | **Улучшенная отзывчивость** |
| Количество операций | Много | Меньше (батчинг) | **~40% меньше I/O** |

### Параллельные расчеты:

| Операция | До | После | Улучшение |
|----------|-----|-------|-----------|
| RSI для 583 монет | Последовательно | Параллельно + векторизация | **20-40%** |
| С NumPy | Нет | Да | **3-5x быстрее** |

---

## 🔧 Интеграция оптимизаций

### Вариант 1: Постепенная интеграция (рекомендуется)

```python
# В bots_modules/filters.py - замена load_all_coins_rsi
from bot_engine.performance_optimizer import get_performance_optimizer

async def load_all_coins_rsi_optimized():
    """Оптимизированная версия загрузки RSI"""
    optimizer = get_performance_optimizer()
    
    # Используем оптимизированный клиент биржи
    if optimizer.exchange_client:
        # Параллельные запросы к бирже
        requests = [
            {'method': 'GET', 'endpoint': f'/api/candles/{symbol}', 'params': {'interval': '6h'}}
            for symbol in pairs
        ]
        results = await optimizer.request_exchange_batch(requests, max_concurrent=20)
        # Обработка результатов...
```

### Вариант 2: Полная интеграция

```python
# В bots_modules/sync_and_cache.py - замена save_bots_state
from bot_engine.performance_optimizer import get_performance_optimizer

async def save_bots_state_async():
    """Асинхронное сохранение состояния"""
    optimizer = get_performance_optimizer()
    
    # Получаем данные
    with bots_data_lock:
        bots_dict = {...}
        auto_bot_config = {...}
    
    # Используем оптимизированное сохранение
    return await optimizer.save_data_optimized(
        BOTS_STATE_FILE, 
        {'bots': bots_dict, 'config': auto_bot_config},
        "состояние ботов"
    )
```

---

## 📝 Примеры использования

### Пример 1: Оптимизированная загрузка RSI

```python
import asyncio
from bot_engine.performance_optimizer import get_performance_optimizer

async def load_rsi_optimized():
    optimizer = get_performance_optimizer()
    
    # Инициализируем клиент биржи
    await optimizer.initialize_exchange_client('https://api.bybit.com')
    
    # Получаем список монет
    pairs = exchange.get_all_pairs()
    
    # Параллельные запросы свечей
    requests = [
        {'method': 'GET', 'endpoint': '/v5/market/kline', 
         'params': {'symbol': symbol, 'interval': '6', 'limit': 200}}
        for symbol in pairs[:100]  # Первые 100 для примера
    ]
    
    results = await optimizer.request_exchange_batch(requests, max_concurrent=20)
    
    # Пакетный расчет RSI
    prices_list = [extract_prices(result) for result in results if result]
    rsi_values = optimizer.calculate_rsi_batch_optimized(prices_list, period=14)
    
    return rsi_values
```

### Пример 2: Асинхронное сохранение с батчингом

```python
import asyncio
from bot_engine.performance_optimizer import get_performance_optimizer

async def save_multiple_files():
    optimizer = get_performance_optimizer()
    
    # Сохраняем несколько файлов (будут батчиться)
    await optimizer.save_data_optimized('data/rsi_cache.json', rsi_data)
    await optimizer.save_data_optimized('data/bots_state.json', bots_data)
    await optimizer.save_data_optimized('data/mature_coins.json', mature_data)
    
    # Принудительный flush всех ожидающих операций
    await optimizer.flush_all_storage()
```

---

## ⚙️ Настройка оптимизаций

### Включение/выключение:

```python
from bot_engine.performance_optimizer import get_performance_optimizer

# Включить оптимизации
optimizer = get_performance_optimizer(enabled=True)

# Выключить оптимизации (fallback на стандартные функции)
optimizer = get_performance_optimizer(enabled=False)
```

### Настройка параметров:

```python
# Асинхронное хранилище
from bot_engine.async_storage import _batch_timeout, _max_batch_size
_batch_timeout = 3.0  # Увеличить таймаут батча
_max_batch_size = 20  # Увеличить размер батча

# Оптимизированный клиент биржи
optimizer = get_performance_optimizer()
await optimizer.initialize_exchange_client(
    base_url='https://api.bybit.com',
    max_connections=200,  # Больше соединений
    max_connections_per_host=50
)
```

---

## 📈 Мониторинг производительности

### Получение статистики:

```python
optimizer = get_performance_optimizer()
stats = optimizer.get_stats()

print(f"Операций сохранения: {stats['storage_operations']}")
print(f"Запросов к бирже: {stats['exchange_requests']}")
print(f"Расчетов: {stats['calculations']}")

if 'exchange' in stats:
    exchange_stats = stats['exchange']
    print(f"Cache hit rate: {exchange_stats['cache_hit_rate']:.1f}%")
    print(f"Avg request time: {exchange_stats['avg_request_time']:.3f}s")
```

---

## ⚠️ Важные замечания

1. **Требования:**
   - `aiohttp` для асинхронных запросов (уже должен быть)
   - `numpy` опционально для векторных операций (установить: `pip install numpy`)

2. **Обратная совместимость:**
   - Все оптимизации имеют fallback на стандартные функции
   - Можно включать постепенно

3. **Тестирование:**
   - Рекомендуется протестировать на тестовой среде
   - Мониторить использование памяти и CPU

4. **Постепенная миграция:**
   - Начать с асинхронного хранилища (низкий риск)
   - Затем оптимизированный клиент биржи
   - В конце оптимизированные расчеты

---

## 🎯 Рекомендации по внедрению

### Этап 1: Асинхронное хранилище (низкий риск)
- Заменить `save_bots_state()` на асинхронную версию
- Заменить `save_rsi_cache()` на асинхронную версию
- **Ожидаемое улучшение:** 30-50% ускорение сохранения

### Этап 2: Оптимизированный клиент биржи (средний риск)
- Интегрировать в `load_all_coins_rsi()`
- Использовать для запросов свечей
- **Ожидаемое улучшение:** 40-60% ускорение загрузки

### Этап 3: Оптимизированные расчеты (низкий риск)
- Использовать пакетные расчеты RSI
- Включить NumPy для векторных операций
- **Ожидаемое улучшение:** 20-40% (с NumPy до 3-5x)

---

## 📊 Итоговые ожидаемые улучшения

После полной интеграции всех оптимизаций:

- **Загрузка 583 монет:** 2-3 мин → **1-1.5 мин** (40-50% быстрее)
- **Сохранение состояния:** Блокирующее → **Неблокирующее** (30-50% быстрее)
- **Расчеты RSI:** Последовательно → **Параллельно** (20-40% быстрее, с NumPy до 3-5x)
- **Использование CPU:** Лучшее использование всех ядер
- **Отзывчивость системы:** Улучшена за счет неблокирующих операций

**Общее улучшение производительности:** **30-50%** для основных операций

