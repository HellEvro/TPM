# Изменения в логировании конфигурации

## Дата: 15 октября 2025
## Обновлено: 15 октября 2025 (исправление ANSI кодов)

### Описание изменений

Изменена система логирования при сохранении конфигурации через UI. Теперь выводятся **только измененные параметры** зеленым цветом.

### Что изменилось

#### До изменений:
- При сохранении конфигурации выводились ВСЕ параметры
- Было невозможно быстро увидеть, что именно изменилось
- Консоль засорялась множеством логов

#### После изменений:
- ✅ Выводятся **только измененные параметры**
- ✅ Изменения выделены **зеленым цветом**
- ✅ Добавлен счетчик изменений
- ✅ Если изменений нет - выводится информационное сообщение
- ✅ **Человекочитаемые названия** параметров на русском языке

### Примеры вывода

#### Системная конфигурация (при изменении 2 параметров):
```
[CONFIG] ✓ Интервал обновления RSI (сек): 300 → 180    (зеленым)
[CONFIG] ✓ Режим отладки: False → True    (зеленым)
[CONFIG] ✅ Изменено параметров: 2, конфигурация сохранена    (зеленым)
```

#### Auto Bot конфигурация (при изменении параметров):
```
[CONFIG] ✓ Максимум одновременных ботов: 5 → 10    (зеленым)
[CONFIG] ✓ Размер позиции по умолчанию (USDT): 10 → 50    (зеленым)
[CONFIG] ✓ Фильтр DOWN тренда для LONG: False → True    (зеленым)
[CONFIG] ✅ Auto Bot: изменено параметров: 3, конфигурация сохранена    (зеленым)
```

#### Если изменений нет:
```
[CONFIG] ℹ️  Изменений не обнаружено
```

#### Включение Auto Bot (только при реальном изменении):
```
[CONFIG] ✓ enabled: False → True
[CONFIG] ✅ Auto Bot: изменено параметров: 1, конфигурация сохранена
================================================================================
🟢 AUTO BOT ВКЛЮЧЕН! 🟢
================================================================================
⚠️  ВНИМАНИЕ: Автобот будет автоматически создавать ботов!
⚙️  Макс. одновременных ботов: 5
📊 RSI пороги: LONG≤29, SHORT≥71
⏰ RSI Time Filter: ON (8 свечей)
================================================================================
```

#### Отключение Auto Bot (только при реальном изменении):
```
[CONFIG] ✓ enabled: True → False
[CONFIG] ✅ Auto Bot: изменено параметров: 1, конфигурация сохранена
================================================================================
🔴 AUTO BOT ВЫКЛЮЧЕН! 🔴
================================================================================
```

### Измененные файлы

1. **bots_modules/api_endpoints.py**
   - Добавлена функция `log_config_change()` - логирует только измененные значения
   - Обновлен endpoint `/api/bots/system-config` - сравнивает старые и новые значения
   - Обновлен endpoint `/api/bots/auto-bot` - сравнивает старые и новые значения
   - Добавлен счетчик изменений `changes_count`
   - Итоговое сообщение с количеством изменений

2. **bot_engine/api/endpoints_config.py**
   - Добавлена функция `log_config_change()` (для будущей миграции на bot_engine)
   - Обновлены endpoints аналогично bots_modules

### Технические детали

- Используется ANSI код `\033[92m` для зеленого цвета (через `print()`)
- Используется ANSI код `\033[91m` для красного цвета (отключение Auto Bot)
- Используется символ `→` для отображения изменения
- Формат вывода: `[CONFIG] ✓ <описание>: <старое значение> → <новое значение>`
- Функция `log_config_change()` возвращает `True` если было изменение, `False` если нет
- **Важно**: ANSI коды выводятся через `print()` вместо `logger.info()`, так как логгер экранирует escape-последовательности
- Блок "AUTO BOT ВКЛЮЧЕН/ВЫКЛЮЧЕН" показывается **только при реальном изменении** статуса enabled
- **Человекочитаемые названия**: Добавлен словарь `CONFIG_NAMES` с понятными названиями параметров на русском языке

#### Словарь CONFIG_NAMES

Содержит соответствия между техническими названиями переменных и их понятными описаниями:

```python
CONFIG_NAMES = {
    'enabled': 'Auto Bot включен',
    'default_position_size': 'Размер позиции по умолчанию (USDT)',
    'rsi_long_threshold': 'RSI порог для LONG',
    'avoid_down_trend': 'Фильтр DOWN тренда для LONG',
    'rsi_update_interval': 'Интервал обновления RSI (сек)',
    # ... и другие параметры
}
```

Если параметр не найден в словаре, используется техническое название.

### Исправления (15 октября 2025)

**Проблема 1: ANSI коды отображались буквально**
- Было: `92m[CONFIG] ✓ enabled: False → True0m`
- Стало: зеленый текст `[CONFIG] ✓ enabled: False → True`
- Решение: использование `print()` вместо `logger.info()` для ANSI кодов

**Проблема 2: Блок "AUTO BOT ВКЛЮЧЕН" показывался постоянно**
- Было: блок выводился при каждом сохранении конфигурации
- Стало: блок выводится **только при реальном изменении** enabled с False на True
- Решение: проверка `old_config.get('enabled') == False and data['enabled'] == True`

**Улучшение 3: Добавлены человекочитаемые названия**
- Было: `default_position_size: 10 → 50`
- Стало: `Размер позиции по умолчанию (USDT): 10 → 50`
- Решение: словарь `CONFIG_NAMES` с русскими названиями всех параметров

### Обратная совместимость

Изменения полностью обратно совместимы:
- API endpoints не изменились
- Формат JSON запросов/ответов остался прежним
- Изменилось только консольное логирование

