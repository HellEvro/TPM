# 🤖 ИИ Определение Оптимальной Точки Входа

## Что это?

ИИ анализирует, **когда лучше входить в позицию**: сразу после прохождения фильтров или подождать лучшей цены.

## Как работает?

### 1. Все фильтры пройдены ✅
- RSI достиг пороговых значений (71 для SHORT, 29 для LONG)
- Монета зрелая
- Exit Scam фильтры пройдены
- Тренд разрешает вход

### 2. ИИ анализирует:
- **Текущая цена** vs **оптимальная цена** (локальные экстремумы)
- **Отклонение** от оптимальной цены
- **Исторические данные** этой монеты (есть ли паттерн "ждать")
- **Волатильность** и **тренд**

### 3. Принимает решение:
```python
# Вход СРАЗУ если:
- Отклонение от оптимальной цены < 2%
- Цена близка к локальному экстремуму

# Ждем если:
- Отклонение > 3% (цена слишком далеко от оптимальной)
- Исторические данные показывают, что эта монета лучше работает при задержке
```

## Настройка

### Включение/отключение

**Файл конфигурации**: `data/default_auto_bot_config.json`

```json
{
  "ai_optimal_entry_enabled": false  // ← Выкл. по умолчанию
}
```

### Почему по умолчанию ОТКЛЮЧЕНО?
- Может мешать на первых порах входить в сделки
- Нужны данные для обучения (минимум 10+ сделок с этой монетой)
- Лучше сначала накопить опыт, затем включить

## Сбор данных для обучения

### 📊 Даже при ОТКЛЮЧЕННОЙ функции:
```python
# Если есть премиум лицензия:
✅ Собираются данные для ИИ (цена, RSI, свечи, таймстамп)
✅ Данные сохраняются в: data/ai/training/{symbol}_entry_data.json
✅ Обучается ML модель на основе результатов
❌ НО: ИИ НЕ используется для фильтрации входа
```

### Результат:
- Накапливаются данные о том, когда вход был оптимальным
- ИИ учится на реальных результатах
- Когда включите функцию → ИИ уже знает паттерны!

## Примеры работы

### Пример 1: Рекомендует ЖДАТЬ
```
[OG] ⏳ ИИ рекомендует подождать: Цена выше оптимальной на 3.2%, ожидаем отката
```

### Пример 2: Рекомендует ВХОД
```
[OG] ✅ ИИ рекомендует вход: Цена близка к оптимальной (+0.8%)
```

### Пример 3: Рекомендует ЖДАТЬ (исторический паттерн)
```
[OG] ⏳ ИИ рекомендует подождать: Исторические данные показывают: лучше подождать (15мин)
```

## Когда включать?

### ✅ Рекомендуется включать:
- Набрано 20+ сделок с монетой
- Хочется улучшить качество входов
- Есть премиум лицензия

### ❌ Не рекомендуется:
- Первые дни работы с системой
- Нет достаточного количества данных
- Нужна быстрая реакция на сигналы

## Статистика обучения

ИИ собирает:
- Символ монеты
- Цену входа
- RSI в момент входа
- Историю свечей (последние 10)
- Направление (LONG/SHORT)
- Таймстамп входа
- Результат сделки (при закрытии)

## Машинное обучение

Когда накопится 20+ примеров → модель автоматически дообучается:
1. Анализирует, какие входы были успешными
2. Определяет паттерны (волатильность, тренд, RSI)
3. Улучшает предсказания

## Важно!

- 🎫 Требуется **премиум лицензия**
- 📊 Данные собираются **автоматически** (даже если функция выкл.)
- 🤖 ИИ используется **только** если `ai_optimal_entry_enabled: true`
- ⏱️ Оценка оптимального входа: **0-30 минут задержки**
