# Статус InfoBot — что доделать и где продолжать

> Для разработчика, который подхватит проект или продолжит настройку на слабом ПК.

---

## Контекст

- **Слабый ПК**: ~4 ядра, `AI_MEMORY_LIMIT_MB=4096` (или ниже)
- **Симптомы**: таймауты RSI, зависания на этапе 4 (анализ трендов), «Сервис ботов недоступен» то появляется, то пропадает
- **Цель**: стабильная работа на слабом железе

---

## Что уже сделано (2026-02)

### 1. RSI aggressive mode

- **Файл**: `patches/patches/012_rsi_aggressive_low_resource.py`, `bots_modules/filters.py`, `bot_engine/config_loader.py`
- При `AI_MEMORY_LIMIT_MB <= 4096`: 2 воркера, батч 200, timeout 90 с

### 2. Этапы 3–7 в фоне

- **Файл**: `bots_modules/continuous_data_loader.py`
- Этапы 3–7 (зрелость, тренды, тяжёлые фильтры, передача автоботу) идут в отдельном потоке
- Цикл 1→2 не ждёт их завершения
- `first_round_complete` выставляется после этапа 2 (RSI готов)

### 3. Этап 4 — анализ трендов

- **Файл**: `bots_modules/filters.py`
- Предзагрузка `individual_coin_settings` одним блоком (без 100+ вызовов `get_individual_coin_settings`)
- Свечи только из `candles_cache` — без API, если передаётся `candles_data` (не `None`)

### 4. Тренды не идут в API

- **Файл**: `bots_modules/filters.py` (вызов), `bots_modules/calculations.py` (логика)
- Раньше при пустом `candles` передавался `None` → `analyze_trend` ходил в `get_chart_data` (API)
- Сейчас передаётся `candles` как есть; пустой список → `return None` без запроса

### 5. UnboundLocalError в загрузке свечей

- **Файл**: `bots_modules/filters.py`, `load_all_coins_candles_fast`
- Добавлен `from bots_modules.imports_and_globals import rsi_data_lock, coins_rsi_data` перед сохранением кэша
- Ошибка «cannot access local variable 'coins_rsi_data'» при `reduced_mode=True` устранена

### 6. Таймауты и retry

- **Файл**: `app.py`, `static/js/managers/bots_manager/04_service.js`
- Таймаут проверки сервиса: 5 → 15 с
- Retry: при ошибке — повторная попытка через 2 с
- `bots_data_lock`: таймаут 5 → 15 с в `load_all_coins_candles_fast`

### 7. Логирование для диагностики

- **Файл**: `app.py`, `bots_modules/api_endpoints.py`
- POST auto-bot: лог в app.py и bots.py при изменении `enabled`

---

## Что сделано дополнительно (2026-02-24)

### 8. Этапы 3–7 выполняются всегда
- **Файл**: `bots_modules/continuous_data_loader.py`
- Этапы 3–6 (зрелость, тренды, тяжёлые фильтры, обработка) теперь запускаются **всегда**, не только при `auto_bot_enabled`
- Этап 7 (передача монет автоботу) — только при включённом автоботе
- **Причина**: UI нужны тренды и результаты тяжёлых фильтров; ранее этапы 4–7 не выполнялись при выключенном автоботе

### 9. Диагностика и фикс analyze_trends_for_signal_coins
- **Файл**: `bots_modules/filters.py`
- Снапшот `coins_rsi_data` под `rsi_data_lock` — избегаем RuntimeError при изменении dict во время итерации
- Атомарная запись обновлений с блокировкой
- **Переставлен порядок locks**: сначала `rsi_data_lock` (снапшот символов и candles_cache), затем кратко `bots_data_lock` (только копирование `individual_coin_settings`). Раньше `bots_data_lock` держался на время итерации по 552 символам → таймаут 15 с у `load_all_coins_candles_fast`.
- Дополнительное логирование в `_run_stages_3_to_7`: traceback при ошибке, лог «ЭТАПЫ 3–7 ЗАВЕРШЕНЫ УСПЕШНО», этапы 5–6

### 10. UI: retry для checkBotsService
- **Файл**: `static/js/managers/bots_manager/04_service.js`
- MAX_RETRIES 1 → 2 (при пиковой нагрузке меньше мигания «Сервис недоступен»)

---

## Что ещё нужно проверить / доделать

### 1. Связь фронт ↔ бэк

- **Симптом**: переключение автобота в UI не отражается в консоли bots.py
- **Диагностика**:
  - Запущены оба процесса: `python app.py` (5000) и `python bots.py` (5001)
  - UI открыт по `http://localhost:5000`
  - В браузере F12 → Console: при клике на переключатель должно быть `[BotsManager] 🤖 ИЗМЕНЕНИЕ ПЕРЕКЛЮЧАТЕЛЯ`
  - Вкладка Network: POST на `/api/bots/auto-bot`, статус ответа
- **Места в коде**: `static/js/managers/bots_manager/10_configuration.js` — `initializeGlobalAutoBotToggle`, `BOTS_SERVICE_URL` в `00_core.js`

### 2. «Сервис недоступен» (мигание)

- **Причина**: при пиковой нагрузке (RSI, этапы 3–7) bots.py может отвечать медленнее
- **Сделано**: таймаут 15 с, retry
- **Если всё ещё мигает**: можно повысить таймаут, добавить ещё один retry или снизить нагрузку (интервал, батчи)

### 3. bots_data_lock timeout на слабом ПК

- **Сообщение**: `⚠️ bots_data_lock timeout 15с — загружаем свечи в full mode`
- **Смысл**: не удалось взять lock за 15 с (конкуренция с этапами 3–7, startup_sync, Auto Save)
- **Идеи**: поднять таймаут, реже вызывать `get_individual_coin_settings`, сократить время удержания lock в фоновых этапах

### 4. Производительность этапа 4 (тренды)

- **Файл**: `bots_modules/filters.py`, `analyze_trends_for_signal_coins`
- Свечи берутся из `coins_rsi_data['candles_cache']`
- Если кэш пуст (другой ТФ, race) — возможен fallback в API; стоит проверить, что `current_timeframe` совпадает с загруженными данными

### 5. Тяжёлые фильтры (этап 5)

- **Файл**: `bots_modules/filters.py`, `apply_heavy_filters_to_coins`
- `time_filter`, `exit_scam`, `loss_reentry` — потенциально тяжёлые
- В RSI-батче они отложены; на этапе 5 считаются только для монет с сигналом

---

## Структура цикла загрузки (1–7)

| Этап | Что делает | Где |
|------|------------|-----|
| 1 | Загрузка свечей | `load_all_coins_candles_fast` (filters.py) |
| 2 | Расчёт RSI | `process_rsi_update` (filters.py) |
| 3 | Зрелость | `_calculate_maturity` (continuous_data_loader.py) |
| 4 | Анализ трендов | `analyze_trends_for_signal_coins` (filters.py) |
| 5 | Тяжёлые фильтры | `apply_heavy_filters_to_coins` (filters.py) |
| 6 | Обработка фильтров | `_process_filters` |
| 7 | Передача автоботу | `_set_filtered_coins_for_autobot` |

Этапы 1–2 идут в основном потоке, 3–7 — в фоновом. Этапы 3–7 зависят от результата 1–2.

---

## Важные пути

- `bots_modules/continuous_data_loader.py` — цикл 1–7
- `bots_modules/filters.py` — RSI, тренды, тяжёлые фильтры
- `bots_modules/calculations.py` — `analyze_trend`, `analyze_trend_6h`
- `bots_modules/api_endpoints.py` — API, в т.ч. `/api/bots/auto-bot`
- `app.py` — прокси на bots.py, `call_bots_service`
- `static/js/managers/bots_manager/` — UI, переключатель автобота, проверка сервиса

---

## Документация

- **`docs/CURSOR_RULES.md`** — правила Cursor (Git, коммиты, чеклист, язык). Скопируй в настройки Cursor.
- **`docs/АНАЛИЗ_И_РЕШЕНИЕ_1000_СДЕЛОК.md`** — анализ и решение по лимиту 1000 сделок.

## Скрипты диагностики

- `scripts/diagnose_rsi_timeout.py` — проверка RSI-батча (воркеры, таймаут, батч)

---

## Конфиг для слабого ПК

В `configs/bot_config.py`:

- `AI_MEMORY_LIMIT_MB = 4096` (или меньше) → включается RSI aggressive
- При необходимости можно уменьшить `max_concurrent`, `check_interval`

---

*Документ обновлён: 2026-02-24*
