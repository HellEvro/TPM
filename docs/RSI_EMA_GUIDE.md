# 📊 RSI И EMA - ПОЛНОЕ РУКОВОДСТВО

**Объединенное руководство по индикаторам RSI и EMA в торговой системе**

---

## 📋 СОДЕРЖАНИЕ

1. [Улучшенный RSI](#улучшенный-rsi)
2. [Умный EMA](#умный-ema)
3. [Пересечения EMA](#пересечения-ema)
4. [Интеграция RSI](#интеграция-rsi)
5. [Интеграция EMA](#интеграция-ema)
6. [Отображение EMA](#отображение-ema)

---

## 📈 УЛУЧШЕННЫЙ RSI

### Основные возможности:
- **RSI на 6-часовом таймфрейме** для автобота
- **Умный кэш RSI данных** для быстрого доступа
- **Автоматическая загрузка** исторических данных
- **Инкрементальная загрузка** с автоматическим отображением монет по мере обработки

### Пороги RSI:
```python
RSI_LONG_THRESHOLD = 29   # Вход в LONG
RSI_SHORT_THRESHOLD = 71  # Вход в SHORT
RSI_EXIT_LONG = 65        # Выход из LONG
RSI_EXIT_SHORT = 35       # Выход из SHORT
```

### Файлы кэша:
- `data/rsi_cache.json` - кэш RSI данных
- Автоматическое обновление каждые 6 часов

---

## 🎯 УМНЫЙ EMA

### Возможности:
- **Динамические периоды EMA** для каждой монеты
- **Оптимальные EMA** на основе исторического анализа
- **Адаптивные сигналы** в зависимости от волатильности

### Реализация:
```python
from bot_engine.indicators import calculate_ema

# Расчет EMA
ema_fast = calculate_ema(prices, period=12)
ema_slow = calculate_ema(prices, period=26)

# Проверка пересечения
if ema_fast[-1] > ema_slow[-1]:
    signal = "BULLISH"
```

---

## 🔄 ПЕРЕСЕЧЕНИЯ EMA

### Сигналы:
- **Золотой крест:** Быстрая EMA пересекает медленную снизу вверх → LONG
- **Мертвый крест:** Быстрая EMA пересекает медленную сверху вниз → SHORT

### Подтверждение:
- Пересечение + RSI в экстремальной зоне = сильный сигнал
- Пересечение без подтверждения RSI = слабый сигнал

---

## 🔌 ИНТЕГРАЦИЯ RSI

### Загрузка данных:
```python
from bot_engine.smart_rsi_manager import SmartRSIManager

# Инициализация
rsi_manager = SmartRSIManager(exchange)

# Запуск
rsi_manager.start()

# Получение данных
rsi_data = rsi_manager.get_rsi_data("BTCUSDT")
```

### API endpoints:
- `GET /api/bots/coins-with-rsi` - список монет с RSI
- `POST /api/bots/force-rsi-update` - принудительное обновление

---

## 🔌 ИНТЕГРАЦИЯ EMA

### Расчет EMA в боте:
```python
from bot_engine.indicators import get_trend_ema

# Получение тренда
trend = get_trend_ema(symbol, exchange)
# Возвращает: "UP", "DOWN", "NEUTRAL"
```

### Оптимальные периоды:
```python
# Загрузка из файла
optimal_ema = load_optimal_ema()
periods = optimal_ema.get(symbol, {"fast": 12, "slow": 26})
```

---

## 📊 ОТОБРАЖЕНИЕ EMA

### Frontend:
- **График EMA** на странице монеты
- **Индикаторы пересечения** в реальном времени
- **Визуализация трендов** (UP/DOWN/NEUTRAL)

### API:
- `GET /api/ema/<symbol>` - данные EMA для монеты
- `GET /api/ema/crossovers` - последние пересечения

---

## 🎯 ИТОГ

Интеграция RSI и EMA обеспечивает:
- ✅ Точные сигналы входа/выхода
- ✅ Подтверждение сигналов RSI трендами EMA
- ✅ Адаптивная торговая логика
- ✅ Визуализация индикаторов

**Система использует комбинацию RSI и EMA для максимально точных торговых решений.**
