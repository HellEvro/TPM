# Отслеживание использованных параметров обучения

## Проблема

При обучении AI система генерирует случайные комбинации RSI параметров для разнообразия. Однако:

1. **Нет отслеживания использованных комбинаций** - одни и те же параметры могут использоваться многократно
2. **Параметры не бесконечны** - существует ограниченное количество возможных комбинаций
3. **Когда параметры закончатся** - система будет повторять уже использованные комбинации

## Решение

Создан модуль `AIParameterTracker`, который:

1. **Отслеживает все использованные комбинации параметров**
2. **Проверяет дубликаты** перед обучением
3. **Предупреждает когда параметры исчерпаны**
4. **Предлагает стратегию** когда все комбинации использованы

## Где сохраняются использованные параметры

**Файл:** `data/ai/used_training_parameters.json`

**Структура:**
```json
{
  "last_update": "2024-01-15T10:30:00",
  "total_combinations": 16777216,
  "used_count": 1250,
  "used_combinations": {
    "abc123def456...": {
      "rsi_params": {
        "oversold": 29,
        "overbought": 71,
        "exit_long_with_trend": 65,
        "exit_long_against_trend": 60,
        "exit_short_with_trend": 35,
        "exit_short_against_trend": 40
      },
      "training_seed": 123456,
      "used_at": "2024-01-15T10:30:00",
      "win_rate": 82.5,
      "symbol": "BTCUSDT"
    }
  }
}
```

## Сколько всего комбинаций параметров?

### Теоретически:
- RSI_OVERSOLD: 20-35 (16 значений)
- RSI_OVERBOUGHT: 65-80 (16 значений)
- RSI_EXIT_LONG_WITH_TREND: 55-70 (16 значений)
- RSI_EXIT_LONG_AGAINST_TREND: 50-65 (16 значений)
- RSI_EXIT_SHORT_WITH_TREND: 25-40 (16 значений)
- RSI_EXIT_SHORT_AGAINST_TREND: 30-45 (16 значений)

**Итого: 16^6 = 16,777,216 комбинаций**

### Практически:
Из-за ограничений и вариации реальное количество меньше, но все равно очень большое.

## Что происходит когда параметры исчерпаны?

### 1. Предупреждение при 95% использовании
Система предупреждает когда использовано 95% всех комбинаций:
```
⚠️ Использовано 95.2% всех комбинаций параметров!
💡 Рекомендуется переключиться на обучение на реальных сделках
```

### 2. Стратегии когда параметры закончились:

#### Вариант A: Переобучение на лучших параметрах
- Система находит лучшие комбинации (по Win Rate)
- Переобучается на них с новыми данными
- Это позволяет улучшить модели без новых параметров

#### Вариант B: Расширение диапазона параметров
- Увеличить `variation_range` с 3 до 5
- Расширить границы параметров (например, RSI_OVERSOLD: 15-40 вместо 20-35)
- Это создаст новые комбинации

#### Вариант C: Более тонкая градация
- Уменьшить шаг вариации (например, ±1 вместо ±3)
- Это создаст больше комбинаций в том же диапазоне

#### Вариант D: Переключение на реальные сделки
- Когда параметры исчерпаны - переключиться на обучение ТОЛЬКО на реальных сделках
- Реальные сделки дают бесконечное разнообразие паттернов
- Это лучший вариант для долгосрочного обучения

## Как проверить статистику использования

### Через код:
```python
from bot_engine.ai.ai_parameter_tracker import AIParameterTracker

tracker = AIParameterTracker()
stats = tracker.get_usage_stats()

print(f"Использовано: {stats['used_combinations']}")
print(f"Всего: {stats['total_combinations']}")
print(f"Осталось: {stats['remaining_combinations']}")
print(f"Процент: {stats['usage_percentage']:.2f}%")
print(f"Исчерпано: {stats['is_exhausted']}")
```

### Через файл:
```bash
cat data/ai/used_training_parameters.json | jq '.used_count, .total_combinations'
```

## Лучшие параметры

Система отслеживает Win Rate для каждой комбинации параметров. Можно получить лучшие:

```python
tracker = AIParameterTracker()
best_params = tracker.get_best_params(limit=10)

for params in best_params:
    print(f"Win Rate: {params['win_rate']:.1f}%")
    print(f"Параметры: {params['rsi_params']}")
    print(f"Использовано: {params['used_at']}")
```

## Сброс использованных параметров

Если нужно начать отслеживание заново:

```python
tracker = AIParameterTracker()
tracker.reset_used_params(confirm=True)
```

**Внимание:** Это удалит всю историю использованных параметров!

## Рекомендации

1. **Мониторить статистику** - проверять процент использования периодически
2. **При 80% использования** - начать подготовку к переключению на реальные сделки
3. **При 95% использования** - переключиться на обучение на реальных сделках
4. **Использовать лучшие параметры** - переобучаться на топ-10 комбинациях периодически
5. **Не сбрасывать историю** - она помогает избежать дубликатов и найти лучшие параметры

