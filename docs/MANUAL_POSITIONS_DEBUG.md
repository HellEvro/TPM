# Отладка отображения ручных позиций

**Дата:** 15 октября 2025  
**Статус:** В процессе отладки

## Добавлено детальное логирование

Для выявления проблемы с отображением ручных позиций добавлено детальное логирование в Backend и Frontend.

## Как проверить

### 1. Перезапустите сервис bots.py

```bash
# Остановите текущий процесс (Ctrl+C)
# Запустите заново
python bots.py
```

### 2. Откройте консоль браузера (F12)

В Chrome/Edge: F12 → вкладка Console

### 3. Откройте страницу Bots в UI

Перейдите на страницу с ботами и посмотрите логи

## Что проверить

### Backend логи (в терминале где запущен bots.py)

При загрузке данных RSI вы должны увидеть:

```
[MANUAL_POSITIONS] 📊 Всего позиций на бирже: 1
[MANUAL_POSITIONS] 🤖 Ботов в системе: 0
[MANUAL_POSITIONS] ✋ Ручных позиций БЕЗ ботов: 1
[MANUAL_POSITIONS] 🔍 Список ручных позиций: ['SYMBOL']
```

**Если видите:**
- "Всего позиций на бирже: 0" → Биржа не возвращает позиции
- "Ботов в системе: 1" и "Ручных позиций: 0" → Есть бот для этой монеты (это нормально)
- "Ручных позиций: 1" но нет списка → Проблема в логике

### Frontend логи (в консоли браузера)

При загрузке страницы вы должны увидеть:

```
[BotsManager] ✋ Ручные позиции получены: ['SYMBOL']
[BotsManager] ✋ Всего ручных позиций: 1
[BotsManager] ✋ Монета SYMBOL помечена как ручная позиция
[BotsManager] ✅ Загружено 583 монет с RSI
[BotsManager] ✅ Помечено 1 монет с ручными позициями
[BotsManager] 🎨 Рендер монеты SYMBOL с классом manual-position
```

**Если НЕ видите:**
- "Ручные позиции получены" → API не возвращает данные
- "Монета помечена" → Проблема в сопоставлении символов
- "Рендер с классом manual-position" → Проблема в логике рендера

## Возможные проблемы и решения

### Проблема 1: Backend не видит позиции
**Симптом:** "Всего позиций на бирже: 0"

**Решение:** 
- Проверьте подключение к бирже
- Убедитесь, что позиция действительно открыта
- Проверьте API ключи

### Проблема 2: Все позиции с ботами
**Симптом:** "Ботов в системе: 1" и "Ручных позиций: 0"

**Решение:**
- Это нормально, если у позиции есть бот
- Для теста создайте позицию на бирже вручную (не через систему)

### Проблема 3: Не совпадают символы
**Симптом:** API возвращает позиции, но фронтенд не помечает монеты

**Возможная причина:** 
- Символ на бирже: "BTCUSDT"
- Символ в API: "BTC"
- Символ в RSI данных: "Bitcoin" (например)

**Проверка:**
Посмотрите в консоль:
- Что возвращает API: `['BTC', 'ETH']`
- Какие символы в монетах: `coin.symbol` в логах

### Проблема 4: CSS класс не применяется
**Симптом:** В логах "Рендер с классом manual-position", но визуально не видно

**Решение:**
1. Откройте Elements в DevTools (F12)
2. Найдите элемент монеты с ручной позицией
3. Проверьте, есть ли класс `manual-position` в атрибуте `class`
4. Проверьте CSS для класса `.manual-position`

## Проверка CSS

Если класс есть, но визуально ничего не меняется, проверьте стили:

```css
/* Должно быть в styles.css */
.coin-item.manual-position {
    /* какие-то стили */
}
```

## Дополнительная отладка

### Проверить API напрямую

```bash
curl http://localhost:5001/api/bots/coins-with-rsi | jq .manual_positions
```

Должно вернуть массив символов:
```json
["BTC", "ETH"]
```

### Проверить фильтр в UI

1. Найдите кнопку фильтра "Ручные позиции"
2. Проверьте атрибут `data-filter="manual-position"`
3. Кликните по кнопке и посмотрите в консоль
4. Должно быть: `[BotsManager] 🔍 Применен фильтр: manual-position`

## Изменённые файлы для отладки

### `bots_modules/api_endpoints.py`
- Добавлены INFO логи с деталями о позициях
- Логируется количество позиций, ботов и ручных позиций

### `static/js/managers/bots_manager.js`
- Уровень логирования изменен на 'debug'
- Добавлены console.log для отслеживания потока данных
- Логируется каждая помеченная монета и каждый рендер

## После выявления проблемы

1. Сообщите, что показывают логи
2. Укажите какой из сценариев подходит
3. Мы исправим конкретную проблему

## Отключение debug логов (после исправления)

В `static/js/managers/bots_manager.js` верните:
```javascript
this.logLevel = 'info'; // Было: 'debug'
```

И удалите/закомментируйте временные console.log


