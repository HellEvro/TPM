# 🤖 АВТОМАТИЧЕСКОЕ ОБУЧЕНИЕ ИИ МОДУЛЕЙ

**Дата:** 2025-10-17  
**Версия:** 1.0  
**Статус:** Production Ready

---

## 🎯 НАЗНАЧЕНИЕ

Автоматический тренер (`auto_trainer.py`) - это **фоновый процесс**, который:

1. ✅ **Ежедневно обновляет** исторические данные с биржи
2. ✅ **Еженедельно переобучает** модели на новых данных
3. ✅ **Автоматически применяет** обновленные модели (hot reload)
4. ✅ **Не требует** ручного вмешательства

---

## ⚙️ НАСТРОЙКА

### В `bot_engine/bot_config.py`:

```python
class AIConfig:
    # Основные настройки
    AI_ENABLED = True  # Включить ИИ модули
    
    # Автоматическое обучение
    AI_AUTO_TRAIN_ENABLED = True  # Включить автоматическое обучение
    
    # Первичная настройка
    AI_AUTO_TRAIN_ON_STARTUP = True  # Обучить при старте если модель не найдена
    AI_INITIAL_COINS_COUNT = 100  # Количество монет при первой настройке
    
    # Обновление данных
    AI_AUTO_UPDATE_DATA = True  # Автоматически обновлять данные
    AI_DATA_UPDATE_INTERVAL = 86400  # 24 часа (ежедневно)
    AI_UPDATE_COINS_COUNT = 50  # Топ 50 монет
    
    # Переобучение модели
    AI_AUTO_RETRAIN = True  # Автоматически переобучать
    AI_RETRAIN_INTERVAL = 604800  # 7 дней (еженедельно)
    AI_RETRAIN_HOUR = 3  # 3:00 AM (ночью)
```

---

## 🔄 КАК ЭТО РАБОТАЕТ

### **При первом запуске (если модели нет):**

```
1. Бот запускается
   ↓
2. Auto Trainer проверяет наличие модели
   ↓
3. Модели нет → Запускается первичная настройка
   ↓
4. Собираются данные (топ 100 монет, ~10-15 минут)
   ↓
5. Модель обучается (~1-2 минуты)
   ↓
6. Модель загружается в AI Manager
   ↓
7. Бот готов к работе!
```

### **В процессе работы:**

```
Каждые 24 часа:
  ↓
  Обновление данных (топ 50 монет, ~5 минут)
  ↓
  Данные сохраняются в data/ai/historical/

Каждые 7 дней (в 3:00 AM):
  ↓
  Переобучение модели (~2 минуты)
  ↓
  Hot reload новой модели (без перезапуска!)
  ↓
  Бот продолжает работу с обновленной моделью
```

---

## 📊 РАСПИСАНИЕ ПО УМОЛЧАНИЮ

| Задача | Частота | Время | Длительность |
|--------|---------|-------|--------------|
| Обновление данных | Ежедневно | Любое | ~5 минут |
| Переобучение | Еженедельно | 3:00 AM | ~2 минуты |
| Первичная настройка | При старте | Один раз | ~15 минут |

---

## 🎛️ НАСТРОЙКИ

### **Изменить частоту обновления данных:**

```python
# Каждые 12 часов (2 раза в день)
AI_DATA_UPDATE_INTERVAL = 43200

# Каждые 48 часов (раз в 2 дня)
AI_DATA_UPDATE_INTERVAL = 172800
```

### **Изменить частоту переобучения:**

```python
# Каждые 3 дня
AI_RETRAIN_INTERVAL = 259200

# Каждые 14 дней (2 недели)
AI_RETRAIN_INTERVAL = 1209600

# Каждый месяц (~30 дней)
AI_RETRAIN_INTERVAL = 2592000
```

### **Изменить количество монет:**

```python
# Обновлять больше монет (дольше, но точнее)
AI_UPDATE_COINS_COUNT = 100

# Обновлять меньше монет (быстрее, но менее точно)
AI_UPDATE_COINS_COUNT = 20
```

### **Отключить автообучение:**

```python
AI_AUTO_TRAIN_ENABLED = False  # Только ручное обучение
```

---

## 📝 ЛОГИ

### **При запуске:**

```
[AutoTrainer] ✅ Запущен в фоновом режиме
[AutoTrainer] Расписание:
[AutoTrainer]   - Обновление данных: каждые 24ч
[AutoTrainer]   - Переобучение: каждые 168ч
```

### **При первичной настройке:**

```
[AutoTrainer] ⚠️ Модель не найдена, требуется первичное обучение
[AutoTrainer] 🚀 Запускаем первичное обучение...
[AutoTrainer] Шаг 1/2: Сбор исторических данных...
[AutoTrainer] Шаг 2/2: Обучение модели...
[AutoTrainer] ✅ Первичная настройка завершена
```

### **При обновлении:**

```
[AutoTrainer] 📥 Обновление исторических данных...
[AutoTrainer] ✅ Данные успешно обновлены
```

### **При переобучении:**

```
[AutoTrainer] 🧠 Переобучение модели...
[AutoTrainer] ✅ Модель успешно переобучена
[AutoTrainer] ✅ Модель перезагружена (hot reload)
```

---

## 🛠️ РУЧНОЕ УПРАВЛЕНИЕ

### **Принудительное обновление:**

```python
from bot_engine.ai.auto_trainer import get_auto_trainer

trainer = get_auto_trainer()
trainer.force_update()  # Обновит данные и переобучит модель
```

### **Проверка статуса:**

```python
from bot_engine.ai.auto_trainer import get_auto_trainer

trainer = get_auto_trainer()
status = trainer.get_status()

print(f"Running: {status['running']}")
print(f"Last update: {status['last_data_update']}")
print(f"Last training: {status['last_training']}")
print(f"Next update: {status['next_data_update']}")
print(f"Next training: {status['next_training']}")
```

### **Остановка автотренера:**

```python
from bot_engine.ai.auto_trainer import stop_auto_trainer

stop_auto_trainer()
```

---

## 💡 РЕКОМЕНДАЦИИ

### **Для production:**

1. ✅ **Включите автообучение:** `AI_AUTO_TRAIN_ENABLED = True`
2. ✅ **Первичная настройка:** `AI_AUTO_TRAIN_ON_STARTUP = True`
3. ✅ **Топ 50 монет:** `AI_UPDATE_COINS_COUNT = 50`
4. ✅ **Еженедельное переобучение:** `AI_RETRAIN_INTERVAL = 604800`

### **Для development:**

1. ✅ **Отключите автообучение:** `AI_AUTO_TRAIN_ENABLED = False`
2. ✅ **Ручное управление:** Запускайте скрипты вручную
3. ✅ **Тестирование:** Экспериментируйте с параметрами

---

## ⚠️ ВАЖНО

### **Первый запуск с автообучением:**

При первом запуске бот будет:
1. Собирать данные для 100 монет (~10-15 минут)
2. Обучать модель (~1-2 минуты)

**Итого:** ~15-20 минут до полной готовности!

### **Ресурсы:**

- **CPU:** Средняя нагрузка во время обучения
- **Память:** ~100-200 MB дополнительно
- **Диск:** ~50-100 MB для данных (50 монет)
- **API запросы:** ~150 запросов при обновлении

---

## 🎯 ПРЕИМУЩЕСТВА

1. ✅ **Всегда актуальная модель** - адаптируется к рынку
2. ✅ **Нет ручной работы** - все автоматически
3. ✅ **Hot reload** - без перезапуска бота
4. ✅ **Логирование** - полный контроль процесса

---

**Готово к использованию!** 🚀

