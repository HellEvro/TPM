# AI Overview — InfoBot 1.7

**Дата:** 14 ноября 2025  
**Статус:** Production Ready (3/4 AI модулей активны)  
**Автопроверка:** `python scripts/verify_ai_ready.py` → 10/10

---

## Зачем этот документ

Раньше AI документация была разбита на десятки файлов (guides, technical, development). Здесь собрана только актуальная информация: активные модули, сервисы лаунчера, данные/обучение, интеграция в торговлю, лицензирование и тесты. Если нужно глубже — см. исходный код в `bot_engine/ai/*` и `ai.py`.

---

## Активные AI модули

| Модуль | Файл | Назначение |
| --- | --- | --- |
| Anomaly Detection | `bot_engine/ai/anomaly_detector.py` | IsolationForest, блокирует pump/dump и манипуляции (>15 признаков, обучено на 2‑летней истории 583 монет). |
| LSTM Predictor 1.0 | `bot_engine/ai/lstm_predictor.py` | 6h прогноз направления, подтверждает сигналы Auto Bot, даёт вероятность и confidence. **Использует PyTorch с GPU поддержкой.** |
| Smart Risk Manager | `bot_engine/ai/smart_risk_manager.py` + `risk_manager.py` | Динамические SL/TP, подбор размера позиции, быстрый премиум бэктест. |
| **Parameter Quality Predictor** | `bot_engine/ai/parameter_quality_predictor.py` | **ML модель предсказания качества параметров, генерирует оптимальные параметры вместо случайных.** |
| **AISelfLearning** | `bot_engine/ai/ai_self_learning.py` | **Система настоящего самообучения AI в реальном времени на каждой сделке.** |
| Auto Training & Continuous Learning | `ai.py`, `bot_engine/ai/auto_trainer.py`, `ai_continuous_learning.py` | Сбор данных, переобучение, проверка обновлённых моделей без остановки торгов. |

> Pattern Recognition остаётся в разработке (`bot_engine/ai/pattern_detector.py`).

---

## AI Launcher & сервисы

`ai.py` управляет защищённым `_ai_launcher.pyc` и запускает процессы:

- `AIDataCollector` — постоянный сбор котировок/объёмов в `data/ai/raw/`.
- `AITrainer` / `AIContinuousLearning` — обновление выборок и переобучение моделей (ежедневно/еженедельно).
- `AISelfLearning` — **настоящее самообучение AI в реальном времени на каждой сделке**.
- `AIBacktester` — прогон стратегий на свежих данных перед выкладкой сигнала.
- `AIStrategyOptimizer` — подбор параметров авто/ручных ботов.
- `AIBotManager` — оркестрация запуска и состояние премиум модулей.
- `ParameterQualityPredictor` — ML модель для предсказания качества параметров, генерация оптимальных параметров.

Стандартный запуск: `python ai.py`. Система автоматически запускает все сервисы (data-service, train, scheduler).

---

## Поток данных и обучение

1. **Сбор**: `AIDataCollector` выгружает свечи, объёмы, depth и сохраняет в `data/ai/feeds/`.
2. **Подготовка**: `ai_data_storage.py` агрегирует фичи (volatility, trend strength, funding, sentiment).
3. **Обучение**:
   - **AISelfLearning**: **самообучение в реальном времени на каждой закрытой сделке** (онлайн-обучение, адаптация к рынку).
   - LSTM переобучается еженедельно (`AI_RETRAIN_INTERVAL = 604800`).
   - Anomaly Detection обновляет окно данных ежедневно (`AI_DATA_UPDATE_INTERVAL = 86400`).
   - Smart Risk использует свежие результаты backtest + историю сделок (`bot_engine/bot_history.py`).
4. **Публикация**: модели и scalers раскладываются в `data/ai/models/` и пробрасываются в `bots_modules/filters.py`.
5. **Валидация**: `scripts/verify_ai_ready.py` и `ai_continuous_learning.py` проверяют контрольные метрики, прежде чем включить новую версию.

### Параллельная работа на разных ПК

Система поддерживает параллельный запуск `ai.py` на нескольких компьютерах для ускорения обучения:

- **Автоматическая координация** через таблицу `training_locks` в БД
- **Распределение работы** - каждый процесс обрабатывает только доступные символы
- **Безопасность** - блокировки предотвращают конфликты и дублирование
- **Отказоустойчивость** - истекшие блокировки автоматически очищаются

**Пример:**
```bash
# На ПК 1
cd \\Evromini\projects\InfoBot
python ai.py --mode train

# На ПК 2 (параллельно)
cd \\Evromini\projects\InfoBot
python ai.py --mode train
```

Подробнее: см. `docs/AI_DATABASE_UNC_PATHS.md` → раздел "Параллельная работа на разных ПК"

---

## Применение в торговле

1. `bots_modules/bot_class.py` считает базовые условия (RSI, тренд, фильтры зрелости).
2. Если `ai_enabled=True`, вызывается `ai_runtime.evaluate_signal(symbol, context)`:
   - **AI блокирует сделку**, если confidence ниже `ai_min_confidence` или сигнал `WAIT`.
   - **AI подтверждает сделку**, если сигнал `LONG/SHORT` и confidence ≥ порога.
3. **AISelfLearning** автоматически обучается на результате каждой сделки для улучшения будущих решений.
4. Smart Risk дополняет решение, вычисляя SL/TP, позицию и fast backtest (для премиум лицензий).

Настройки для Auto Bot:
```json
{
  "ai_enabled": true,
  "ai_min_confidence": 0.72,
  "ai_override_original": false
}
```

---

## Конфигурация (фрагмент `bot_engine/bot_config.py`)

```python
class AIConfig:
    AI_ENABLED = True
    AI_ANOMALY_DETECTION_ENABLED = True
    AI_RISK_MANAGEMENT_ENABLED = True
    AI_AUTO_TRAIN_ENABLED = True
    AI_SELF_LEARNING_ENABLED = True      # Самообучение в реальном времени

    AI_DATA_UPDATE_INTERVAL = 86400      # 24 часа
    AI_RETRAIN_INTERVAL = 604800         # 7 дней
    AI_RETRAIN_HOUR = 3                  # 03:00 UTC
    AI_UPDATE_COINS_COUNT = 0            # 0 = все доступные монеты

    AI_ANOMALY_BLOCK_THRESHOLD = 0.7
    AI_RISK_UPDATE_INTERVAL = 300        # 5 минут

    # Настройки самообучения
    AI_SELF_LEARNING_BUFFER_SIZE = 50    # Размер буфера для онлайн обучения
    AI_ADAPTATION_THRESHOLD = 0.1        # Порог изменения для адаптации к рынку
    AI_PERFORMANCE_WINDOW = 50           # Окно для оценки производительности
```

Из UI (`start_infobot_manager` → вкладка AI) доступны флаги включения модулей, минимальная уверенность, расписание retrain и параметры Smart Risk.

---

## Установка и требования

### PyTorch и GPU

⚠️ **ВАЖНО:** PyTorch ОБЯЗАТЕЛЕН для работы LSTM Predictor! Без PyTorch LSTM Predictor не будет работать (даже на CPU).

**LSTM Predictor использует PyTorch** для обучения и предсказаний. PyTorch работает на всех версиях Python 3.8+, включая 3.12 и 3.14+.

**Как работает система:**
- **С GPU:** PyTorch использует GPU для ускорения обучения и предсказаний (быстрее)
- **Без GPU:** PyTorch автоматически использует CPU (работает медленнее, но полностью функционально)
- LSTM Predictor автоматически определяет доступное устройство и использует его

**Автоматическая установка PyTorch (рекомендуется):**

При первом запуске `python ai.py` система автоматически:
- Проверяет наличие PyTorch
- Если PyTorch не установлен: автоматически запускает установку
- Определяет наличие GPU и устанавливает нужную версию (GPU или CPU)
- Продолжает работу после успешной установки

**Ручная установка (опционально, если хотите установить заранее):**
```bash
python scripts/setup_python_gpu.py
```
Скрипт автоматически определяет наличие GPU и устанавливает нужную версию (GPU или CPU).

**Ручная установка:**
```bash
# С GPU (NVIDIA CUDA 12.1) - если есть GPU
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# С GPU (NVIDIA CUDA 11.8) - если CUDA 12.1 не поддерживается
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118

# БЕЗ GPU (CPU версия) - если GPU нет
pip install torch torchvision torchaudio
```

**Проверка установки:**
```bash
python -c "import torch; print('PyTorch:', torch.__version__); print('CUDA:', torch.cuda.is_available()); print('GPU:', torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'CPU (GPU не найден)')"
```

**Требования для GPU (опционально, если хотите ускорение):**
- NVIDIA GPU с поддержкой CUDA
- Драйверы NVIDIA (проверьте: `nvidia-smi`)
- CUDA Toolkit 11.8 или 12.1 (устанавливается автоматически с PyTorch)

> ⚠️ **ВАЖНО:** 
> - PyTorch ОБЯЗАТЕЛЕН для работы LSTM Predictor (даже на CPU)
> - Отдельное виртуальное окружение `.venv_gpu` больше не требуется — PyTorch устанавливается в текущее окружение Python
> - Если GPU нет, система автоматически использует CPU — дополнительная настройка не требуется

---

## Лицензирование и защита

- HWID лицензия (`scripts/activate_premium.py`) генерирует `.lic` в корне.  
- `bot_engine/ai/license_checker.pyc` валидирует лицензию перед запуском премиум сервисов.  
- Защищённые сборки (`license_generator/build_ai_launcher.py`) обновляют `_ai_launcher.pyc`, `_infobot_ai_protected.py` и автозагрузчик в `ai.py`.  
- Без лицензии AI запускается в lite-режиме (Anomaly Detection + базовый Risk Manager). Smart Risk, premium SL/TP и авто backtest отключаются.

---

## Тесты и диагностика

```bash
python scripts/test_ai_initialization.py     # Проверка окружения и моделей
python scripts/test_ai_detector_status.py   # Статус Anomaly Detection
python scripts/test_risk_manager.py         # Расчёты Smart/Dynamic Risk
python scripts/test_full_ai_system.py       # bots.py + ai.py + лицензия
python scripts/verify_ai_ready.py           # Финальная проверка (10/10)
```

Логи: `logs/ai.log`, `logs/bots.log` (фильтр `AI`). Для быстрого статуса откройте `/api/ai/status` (порт 5001).

---

## Roadmap

- ✅ **AISelfLearning**: Система настоящего самообучения AI в реальном времени (завершено).
- 🧭 Завершить Pattern Recognition (CNN, 0/7 задач).
- 🌍 Расширить мульти-биржевой сбор данных (Binance, OKX) в `AIDataCollector`.
- 🤖 Вынести управление лицензиями в Telegram-бот.
- 📊 Добавить визуальные отчёты по backtest/optimizer в Web UI.
- 🔁 Расширить автоматическое тестирование до smoke-suite перед каждым релизом.
- 📈 Добавить расширенную аналитику производительности AI с графиками трендов.

---

**Коротко:** 
1. Установите PyTorch: `python scripts/setup_python_gpu.py`
2. Запускайте `start_infobot_manager` или `python ai.py`
3. Держите `.lic` актуальной
4. Следите за `scripts/verify_ai_ready.py`

AI обеспечит фильтрацию pump/dump, подтверждение сигналов LSTM (с GPU ускорением) и премиальный риск-менеджмент без ручных действий.

---

## 📚 Дополнительная документация

- **Руководство по миграции на SQLite БД:** `docs/AI_DATABASE_MIGRATION_GUIDE.md`
  - Полное описание структуры БД и всех таблиц
  - Примеры использования AIDatabase
  - Руководство по миграции других модулей на БД
  - Best practices и FAQ
  - **Используйте для миграции bots.py и других модулей!**

- **ML Модель Предсказания Качества Параметров:** `docs/AI_ML_PARAMETER_QUALITY_PREDICTOR.md`
  - Полное описание ML модели для генерации оптимальных параметров
  - Технические детали реализации
  - Логика расчета качества параметров
  - Защита от повреждения файлов

- **Работа с UNC путями и параллельная обработка:** `docs/AI_DATABASE_UNC_PATHS.md`
  - Поддержка сетевых путей
  - Координация параллельной работы на разных ПК
  - Настройка и использование

- **Система Самообучения AI:** `bot_engine/ai/ai_self_learning.py`
  - Онлайн-обучение на каждой сделке
  - Адаптация к изменяющимся рыночным условиям
  - Самооценка производительности
  - Непрерывное улучшение эффективности
