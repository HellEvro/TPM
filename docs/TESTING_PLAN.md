# 🧪 ПЛАН КОМПЛЕКСНОГО ТЕСТИРОВАНИЯ СИСТЕМЫ

**Цель:** Проверить работоспособность всех функций и модулей bots.py

---

## 📋 ТЕСТОВЫЕ СЦЕНАРИИ

### 1. 🚀 ТЕСТ ЗАПУСКА И ИНИЦИАЛИЗАЦИИ

#### Что проверяем:
- ✅ Загрузка конфигураций
- ✅ Инициализация биржи
- ✅ Загрузка RSI кэша
- ✅ Запуск воркеров
- ✅ Флаг `system_initialized`

#### Как проверить:
```bash
# 1. Запустить bots.py
python bots.py

# 2. Проверить логи на наличие:
# [INIT] 🎉 СИСТЕМА ПОЛНОСТЬЮ ИНИЦИАЛИЗИРОВАНА И ГОТОВА К ТОРГОВЛЕ!
# [AUTO_BOT] ✅ Система инициализирована, начинаем работу...
# [CONFIG] 🔒 Auto Bot принудительно выключен при запуске

# 3. Проверить API статус
curl http://localhost:5001/api/status
# Ожидаем: {"status": "online", "auto_bot_enabled": false}
```

#### Критерии успеха:
- [ ] Автобот выключен при запуске
- [ ] Воркер ждал инициализации
- [ ] Система полностью инициализирована
- [ ] API отвечает

---

### 2. 🛡️ ТЕСТ ЗАЩИТЫ ОТ РУЧНЫХ ПОЗИЦИЙ

#### Что проверяем:
- ✅ Проверка позиций на бирже
- ✅ Блокировка создания ботов для существующих позиций
- ✅ Логирование предупреждений

#### Как проверить:
```bash
# 1. ВРУЧНУЮ откройте позицию на бирже (например, BTC LONG)

# 2. Включите автобот через UI

# 3. Проверьте логи на наличие:
# [TRADING_RULES] 🚫 BTC: НА БИРЖЕ УЖЕ ЕСТЬ ПОЗИЦИЯ LONG - НЕ СОЗДАЕМ БОТА!

# 4. Проверьте список ботов:
curl http://localhost:5001/api/bots/list
# BTC НЕ должен быть в списке
```

#### Критерии успеха:
- [ ] Система обнаружила позицию на бирже
- [ ] Бот НЕ был создан
- [ ] Предупреждение залогировано
- [ ] Ручная позиция не тронута

---

### 3. 🔄 ТЕСТ HEDGE РЕЖИМА

#### Что проверяем:
- ✅ Разрешение противоположных позиций
- ✅ Блокировка дубликатов одной стороны
- ✅ Корректная проверка `side`

#### Как проверить:
```bash
# 1. Создайте бота для монеты (например, ETH)
curl -X POST http://localhost:5001/api/bots/create \
  -H "Content-Type: application/json" \
  -d '{"symbol": "ETH", "volume_value": 10}'

# 2. Дождитесь открытия LONG позиции

# 3. Вручную на бирже откройте SHORT по ETH

# 4. Проверьте логи:
# [AUTO] ℹ️ ETH: Есть позиция LONG, но сигнал SHORT (противоположная сторона - разрешено)

# 5. Проверьте позиции на бирже - должны быть:
# - ETH LONG (от бота)
# - ETH SHORT (ручная)
```

#### Критерии успеха:
- [ ] LONG и SHORT одновременно существуют
- [ ] Система разрешила противоположную сторону
- [ ] Дубликаты одной стороны блокируются

---

### 4. 🔒 ТЕСТ БЕЗОПАСНОСТИ ИНИЦИАЛИЗАЦИИ

#### Что проверяем:
- ✅ Торговля НЕ начинается до инициализации
- ✅ Воркер ждет флага
- ✅ Проверки в критических функциях

#### Как проверить:
```bash
# 1. Добавьте задержку в init_bot_service() для теста:
# time.sleep(10)  # в конце функции перед установкой флага

# 2. Запустите bots.py

# 3. Проверьте логи первых 10 секунд:
# [AUTO_BOT] ⏳ Ожидание полной инициализации системы...
# [AUTO] ⏳ Система еще не инициализирована - пропускаем проверку
# [BOT_SIGNALS] ⏳ Система еще не инициализирована - пропускаем обработку

# 4. После 10 секунд:
# [INIT] 🎉 СИСТЕМА ПОЛНОСТЬЮ ИНИЦИАЛИЗИРОВАНА И ГОТОВА К ТОРГОВЛЕ!
# [AUTO_BOT] ✅ Система инициализирована, начинаем работу...
```

#### Критерии успеха:
- [ ] Торговля НЕ началась до инициализации
- [ ] Воркер ждал флага
- [ ] После флага все работает

---

### 5. 📊 ТЕСТ ПРОВЕРКИ ЗРЕЛОСТИ

#### Что проверяем:
- ✅ Блокировка незрелых монет
- ✅ Проверка при создании бота
- ✅ Проверка при восстановлении из файла

#### Как проверить:
```bash
# 1. Очистите mature_coins.json:
echo '{"coins": {}}' > data/mature_coins.json

# 2. Добавьте SERAPH в bots_state.json вручную

# 3. Перезапустите bots.py

# 4. Проверьте логи:
# [LOAD_STATE] 🚫 SERAPH: Монета НЕ ЗРЕЛАЯ - НЕ восстанавливаем бота!

# 5. Попробуйте создать бота для SERAPH через API:
curl -X POST http://localhost:5001/api/bots/create \
  -H "Content-Type: application/json" \
  -d '{"symbol": "SERAPH", "volume_value": 10}'

# Ожидаем: {"success": false, "error": "Монета не прошла проверку зрелости"}
```

#### Критерии успеха:
- [ ] SERAPH НЕ восстановился из файла
- [ ] Создание бота заблокировано
- [ ] Предупреждения залогированы

---

### 6. 🎯 ТЕСТ АВТОБОТА

#### Что проверяем:
- ✅ Включение/выключение
- ✅ Создание ботов по сигналам
- ✅ Проверка фильтров
- ✅ Уважение max_concurrent

#### Как проверить:
```bash
# 1. Включите автобот через UI или API:
curl -X POST http://localhost:5001/api/bots/auto-bot \
  -H "Content-Type: application/json" \
  -d '{"enabled": true, "max_concurrent": 3}'

# 2. Дождитесь проверки сигналов (3 минуты)

# 3. Проверьте логи:
# [AUTO] 🔍 Проверка Auto Bot сигналов...
# [AUTO] 🚀 Создаем бота для BTC (RSI: 28.5, сигнал: ENTER_LONG)

# 4. Проверьте список ботов:
curl http://localhost:5001/api/bots/list

# 5. Убедитесь, что создано не более 3 ботов (max_concurrent)
```

#### Критерии успеха:
- [ ] Автобот включается/выключается
- [ ] Боты создаются по сигналам
- [ ] Фильтры работают (зрелость, тренд)
- [ ] max_concurrent соблюдается

---

### 7. 📈 ТЕСТ RSI И SMART RSI MANAGER

#### Что проверяем:
- ✅ Загрузка RSI данных
- ✅ Обновление кэша
- ✅ Плановые обновления
- ✅ API endpoints

#### Как проверить:
```bash
# 1. Проверьте статус Smart RSI:
curl http://localhost:5001/api/bots/smart-rsi/status

# 2. Принудительное обновление:
curl -X POST http://localhost:5001/api/bots/force-rsi-update

# 3. Проверьте логи:
# [SMART_RSI] 📊 Плановое обновление RSI данных...
# [RSI] 🔄 Начинаем загрузку RSI 6H для всех монет...

# 4. Получите список монет с RSI:
curl http://localhost:5001/api/bots/coins-with-rsi

# Ожидаем: список монет с RSI значениями
```

#### Критерии успеха:
- [ ] RSI кэш загружается
- [ ] Плановые обновления работают
- [ ] Принудительное обновление работает
- [ ] API возвращает корректные данные

---

### 8. 🔄 ТЕСТ СИНХРОНИЗАЦИИ ПОЗИЦИЙ

#### Что проверяем:
- ✅ Синхронизация каждые 3 секунды (frontend)
- ✅ Обновление PnL
- ✅ Обнаружение закрытых позиций

#### Как проверить:
```bash
# 1. Создайте бота и дождитесь открытия позиции

# 2. Вручную на бирже закройте позицию

# 3. Через 3-10 секунд проверьте статус бота:
curl http://localhost:5001/api/bots/list

# Статус должен измениться на IDLE

# 4. Проверьте логи:
# [SYNC_EXCHANGE] ✅ Синхронизировано 0 ботов
# [BOT] Позиция закрыта на бирже, обновляем статус
```

#### Критерии успеха:
- [ ] Синхронизация обнаружила закрытие
- [ ] Статус бота обновился
- [ ] PnL корректно рассчитан

---

### 9. 💾 ТЕСТ АВТОСОХРАНЕНИЯ

#### Что проверяем:
- ✅ Сохранение каждые 30 секунд
- ✅ Корректность данных
- ✅ Восстановление после перезапуска

#### Как проверить:
```bash
# 1. Создайте 2-3 бота

# 2. Дождитесь автосохранения (30 сек)
# Лог: [AUTO_SAVE] 💾 Автосохранение состояния 3 ботов...

# 3. Проверьте файл:
cat data/bots_state.json
# Должны быть все боты

# 4. Перезапустите bots.py

# 5. Проверьте восстановление:
# [LOAD_STATE] ✅ Восстановлено ботов: 3
```

#### Критерии успеха:
- [ ] Автосохранение работает
- [ ] Данные корректны
- [ ] Восстановление работает

---

### 10. 🛑 ТЕСТ GRACEFUL SHUTDOWN

#### Что проверяем:
- ✅ Корректная остановка
- ✅ Сохранение состояния
- ✅ Остановка воркеров

#### Как проверить:
```bash
# 1. Запустите bots.py

# 2. Нажмите Ctrl+C

# 3. Проверьте логи:
# [SHUTDOWN] 🛑 Получен сигнал 2, начинаем graceful shutdown...
# [AUTO_BOT] 🛑 Auto Bot Worker остановлен
# [AUTO_SAVE] 💾 Auto Save Worker остановлен
# [CLEANUP] 🛑 Флаг system_initialized сброшен
# [CLEANUP] 🧹 Очистка ресурсов сервиса ботов...

# 4. Проверьте, что процесс завершился (не висит)
```

#### Критерии успеха:
- [ ] Остановка корректная
- [ ] Состояние сохранено
- [ ] Воркеры остановлены
- [ ] Процесс завершен

---

## 🎯 БЫСТРЫЙ ЧЕКЛИСТ

### Минимальный набор тестов:
```bash
# 1. Запуск
python bots.py
# ✅ Автобот выключен, система инициализирована

# 2. Проверка API
curl http://localhost:5001/api/status
# ✅ {"status": "online"}

# 3. Проверка защиты от ручных позиций
# Откройте позицию вручную → включите автобот → проверьте логи
# ✅ Бот НЕ создан для ручной позиции

# 4. Проверка зрелости
# Добавьте SERAPH в bots_state.json → перезапустите
# ✅ SERAPH НЕ восстановился

# 5. Остановка
# Ctrl+C
# ✅ Graceful shutdown
```

---

## 📊 АВТОМАТИЗИРОВАННОЕ ТЕСТИРОВАНИЕ

### Создание тестового скрипта:

```python
# test_system.py
import requests
import time
import json

BASE_URL = "http://localhost:5001"

def test_api_status():
    """Тест 1: API статус"""
    r = requests.get(f"{BASE_URL}/api/status")
    assert r.status_code == 200
    data = r.json()
    assert data['status'] == 'online'
    assert data['auto_bot_enabled'] == False
    print("✅ Test 1: API Status - PASSED")

def test_bots_list():
    """Тест 2: Список ботов"""
    r = requests.get(f"{BASE_URL}/api/bots/list")
    assert r.status_code == 200
    data = r.json()
    assert 'data' in data
    print(f"✅ Test 2: Bots List - PASSED ({len(data['data'])} bots)")

def test_auto_bot_config():
    """Тест 3: Конфигурация автобота"""
    r = requests.get(f"{BASE_URL}/api/bots/auto-bot")
    assert r.status_code == 200
    data = r.json()
    assert data['data']['enabled'] == False
    print("✅ Test 3: Auto Bot Config - PASSED")

def test_coins_with_rsi():
    """Тест 4: Монеты с RSI"""
    r = requests.get(f"{BASE_URL}/api/bots/coins-with-rsi")
    assert r.status_code == 200
    data = r.json()
    assert 'coins' in data
    print(f"✅ Test 4: Coins with RSI - PASSED ({data['count']} coins)")

if __name__ == "__main__":
    print("🧪 Запуск автоматизированных тестов...\n")
    
    try:
        test_api_status()
        test_bots_list()
        test_auto_bot_config()
        test_coins_with_rsi()
        
        print("\n🎉 ВСЕ ТЕСТЫ ПРОЙДЕНЫ!")
    except Exception as e:
        print(f"\n❌ ТЕСТ ПРОВАЛЕН: {e}")
```

### Запуск:
```bash
python test_system.py
```

---

## 🎯 ИТОГ

### Для полной проверки выполните:
1. ✅ **Запуск и инициализация** (5 мин)
2. ✅ **Защита от ручных позиций** (10 мин)
3. ✅ **Hedge режим** (15 мин)
4. ✅ **Проверка зрелости** (5 мин)
5. ✅ **Автобот** (10 мин)
6. ✅ **Graceful shutdown** (2 мин)

**Общее время:** ~50 минут

### Или используйте:
- **Быстрый чеклист** (5 мин)
- **Автоматизированные тесты** (1 мин)

**Система будет полностью протестирована!** 🚀
