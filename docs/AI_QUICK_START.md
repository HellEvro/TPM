# 🚀 БЫСТРЫЙ СТАРТ - AI МОДУЛЬ

**Дата:** 2025-10-17  
**Статус:** Готов к тестированию

---

## ✅ ЧТО ГОТОВО

- ✅ Модель обучена на **583 монетах** (2.1M свечей)
- ✅ Размер модели: **796.4 KB**
- ✅ Интегрировано в ExitScam фильтр
- ✅ Автоматическое обучение настроено

---

## 🔧 АКТИВАЦИЯ ЗА 3 ШАГА

### **Шаг 1: Включить режим разработки**

```powershell
# Windows PowerShell
$env:AI_DEV_MODE = "1"
```

или

```cmd
# Windows CMD
set AI_DEV_MODE=1
```

### **Шаг 2: Включить ИИ в конфиге**

Откройте `bot_engine/bot_config.py` и измените:

```python
class AIConfig:
    # Было:
    AI_ENABLED = False
    AI_ANOMALY_DETECTION_ENABLED = False
    
    # Должно быть:
    AI_ENABLED = True
    AI_ANOMALY_DETECTION_ENABLED = True
```

### **Шаг 3: Запустить бота**

```bash
python bots.py
```

---

## ✅ ПРОВЕРКА РАБОТЫ

### **В логах должно появиться:**

```
[AI] 🔧 Режим разработки активирован
[AI] 🎫 Лицензия: developer
[AI] 📅 Действительна до: 9999-12-31
[AI] ✅ Anomaly Detector загружен
[AI] 🎉 Загружено модулей: 1/4
```

### **При обнаружении аномалии:**

```
[EXIT_SCAM] BTC: Анализ последних 10 свечей
[EXIT_SCAM] BTC: ✅ Базовые проверки пройдены
[EXIT_SCAM] BTC: ❌ БЛОКИРОВКА (AI): Обнаружена аномалия PUMP (severity: 85.3%)
```

---

## 📊 МОНИТОРИНГ

### **Что смотреть:**

1. **Количество блокировок**
   - Ищите в логах: `[EXIT_SCAM].*БЛОКИРОВКА (AI)`
   - Если слишком много - увеличьте `AI_ANOMALY_BLOCK_THRESHOLD`

2. **Типы аномалий**
   - PUMP - резкий рост
   - DUMP - резкое падение
   - MANIPULATION - манипуляция

3. **Severity (степень аномалии)**
   - > 90% - очень сильная аномалия
   - 70-90% - средняя (блокируется по умолчанию)
   - < 70% - слабая (предупреждение)

---

## ⚙️ НАСТРОЙКА THRESHOLD

Если нужно изменить чувствительность, откройте `bot_engine/bot_config.py`:

```python
class AIConfig:
    # Строгий режим (блокирует даже слабые аномалии)
    AI_ANOMALY_BLOCK_THRESHOLD = 0.5  # 50%
    
    # Рекомендуется (баланс)
    AI_ANOMALY_BLOCK_THRESHOLD = 0.7  # 70% (по умолчанию)
    
    # Мягкий режим (только сильные аномалии)
    AI_ANOMALY_BLOCK_THRESHOLD = 0.9  # 90%
```

---

## 🔄 АВТОМАТИЧЕСКОЕ ОБУЧЕНИЕ (ОПЦИОНАЛЬНО)

Если хотите автоматическое обновление данных и переобучение:

```python
# bot_engine/bot_config.py

class AIConfig:
    AI_AUTO_TRAIN_ENABLED = True  # Включить автообучение
    AI_DATA_UPDATE_INTERVAL = 86400  # Обновление каждые 24ч
    AI_RETRAIN_INTERVAL = 604800  # Переобучение каждые 7 дней
```

---

**ГОТОВО! ЗАПУСКАЙТЕ!** 🚀

