# 🚀 LSTM Predictor - Быстрый старт

**Для максимальной прибыли (+50-70% к ROI)**

---

## ⚡ БЫСТРАЯ УСТАНОВКА (5 минут)

### Шаг 1: Установить зависимости
```bash
pip install tensorflow>=2.13.0 scikit-learn>=1.3.0
```

### Шаг 2: Собрать данные (если еще нет)
```bash
python scripts/ai/collect_historical_data.py --all
```

### Шаг 3: Обучить LSTM
```bash
python scripts/ai/train_lstm_predictor.py --coins 0 --epochs 50
```

### Шаг 4: Готово! ✅
LSTM автоматически активируется при следующем запуске бота.

---

## 🎯 ЧТО ДЕЛАЕТ LSTM?

LSTM предсказывает движение цены на **6 часов вперед**:

- **Направление**: ↑ вверх или ↓ вниз
- **Изменение**: в процентах (+2.5%, -1.3%)
- **Уверенность**: 0-100% (фильтр слабых сигналов)

### Пример предсказания:
```
[AI] BTC 🧠 LSTM: ↑ ВВЕРХ (+2.5%, уверенность: 75.3%)
[LSTM] BTC: ✅ ПОДТВЕРЖДЕНИЕ: LSTM предсказывает LONG
```

---

## 📊 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

| До LSTM | С LSTM | Улучшение |
|---------|--------|-----------|
| 55-60% точность | 65-75% | **+15%** |
| +30-40% ROI | +50-70% | **+50%** |

---

## ⚙️ НАСТРОЙКА

### Включение/выключение:
```python
# bot_engine/bot_config.py
AI_LSTM_ENABLED = True  # False чтобы выключить
```

### Минимальная уверенность:
```python
AI_LSTM_MIN_CONFIDENCE = 0.6  # 60% (0.5-0.9)
```

### Логирование:
```python
AI_LOG_PREDICTIONS = True  # Видеть все предсказания в логах
```

---

## 🔄 АВТОМАТИЧЕСКОЕ ОБУЧЕНИЕ

LSTM автоматически переобучается:
- ✅ Каждую неделю (7 дней)
- ✅ Данные обновляются ежедневно
- ✅ Hot reload без перезапуска

### Ручное переобучение:
```bash
python scripts/ai/train_lstm_predictor.py
```

---

## 🐛 ПРОБЛЕМЫ?

### LSTM не работает?
```python
# Проверить статус
from bot_engine.ai.ai_manager import get_ai_manager

ai = get_ai_manager()
if ai.lstm_predictor:
    status = ai.lstm_predictor.get_status()
    print(f"Обучена: {status['trained']}")
    print(f"Образцов: {status['training_samples']}")
else:
    print("LSTM не загружен - проверьте TensorFlow")
```

### TensorFlow ошибки?
```bash
# Переустановить TensorFlow
pip uninstall tensorflow
pip install tensorflow==2.13.0
```

### Нет улучшений?
- Подождите 1-2 недели для статистики
- Проверьте `AI_LSTM_MIN_CONFIDENCE` (не слишком высокий?)
- Обучите заново на свежих данных

---

## 📚 ДОПОЛНИТЕЛЬНО

- **Техническая документация**: `docs/ai_technical/LSTM_PREDICTOR.md`
- **Детали реализации**: `docs/ai_development/PHASE_3_LSTM_COMPLETE.md`

---

**Вопросы? Проблемы? Напишите в поддержку!** 💬

