# InfoBot - Раздельная архитектура торговых ботов

## 🏗️ Архитектура

InfoBot теперь работает в виде двух отдельных сервисов:

### 📊 Основное приложение (`app.py`)
- **Порт**: 5000
- **Функции**: 
  - Мониторинг позиций
  - Анализ рынка  
  - Закрытые PnL
  - Статистика
  - Веб-интерфейс

### 🤖 Сервис ботов (`bots.py`)
- **Порт**: 5001  
- **Функции**:
  - Управление торговыми ботами
  - RSI анализ (6H таймфрейм)
  - Автоматические сделки
  - Состояние ботов

## 🚀 Запуск (ручной режим)

### Пошаговые инструкции:

**1. Откройте первый терминал:**
```bash
cd путь/к/InfoBot
python app.py
```
*Должно показать: "Running on http://127.0.0.1:5000"*

**2. Откройте второй терминал:**
```bash
cd путь/к/InfoBot  
python bots.py
```
*Должно показать: "Running on http://127.0.0.1:5001"*

**3. Откройте браузер:**
- Перейдите на http://localhost:5000
- Нажмите на вкладку "Боты"

### Проверка работоспособности:
```bash
python test_setup.py
```

## 🌐 URL и эндпоинты

### Веб-интерфейс
- **Основной сайт**: http://localhost:5000

### API эндпоинты ботов
- `GET /api/bots/list` - Список всех ботов
- `POST /api/bots/control` - Управление ботом
- `GET /api/bots/config` - Конфигурация Auto Bot
- `POST /api/bots/config` - Обновить конфигурацию
- `GET /api/bots/status` - Статус всех ботов
- `GET /api/bots/health` - Проверка сервиса

## 💡 Преимущества разделения

### ✅ Стабильность
- Сбой в ботах не влияет на основной интерфейс
- Независимая отладка компонентов

### ✅ Производительность  
- Торговые операции не блокируют UI
- Параллельная обработка данных

### ✅ Масштабируемость
- Можно запускать несколько экземпляров ботов
- Легко добавлять новые биржи

### ✅ Разработка
- Раздельное тестирование компонентов
- Изолированные логи

## 🔧 Отладка

### Проверка статуса сервисов
```bash
# Основное приложение
curl http://localhost:5000/

# Сервис ботов
curl http://localhost:5001/health
```

### Логи
- **Основное приложение**: Консоль + `logs/app.log`
- **Сервис ботов**: Консоль + `logs/bots.log`

## ⚠️ Важные замечания

1. **Порядок запуска**: Сначала `bots.py`, потом `app.py`
2. **Сервис ботов должен быть доступен** для работы страницы "Боты"
3. **Общие настройки** в `app/config.py` используются обоими сервисами
4. **API ключи биржи** нужны только в сервисе ботов

## 🛠️ Устранение неполадок

### Ошибка "Сервис ботов недоступен"
1. Проверьте, что `bots.py` запущен
2. Убедитесь, что порт 5001 свободен
3. Проверьте логи в консоли

### Боты не отображаются
1. Проверьте соединение с биржей в `bots.py`
2. Убедитесь в правильности API ключей
3. Проверьте логи сервиса ботов
