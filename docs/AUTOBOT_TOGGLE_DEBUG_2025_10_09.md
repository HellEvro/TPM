# 🔍 ОТЛАДКА ПЕРЕКЛЮЧАТЕЛЯ AUTO BOT - 09.10.2025

## 🚨 ПРОБЛЕМА

**Auto Bot не запускается по кнопке!**

---

## 🔧 ДИАГНОСТИКА

### Шаг 1: Проверьте консоль браузера (F12)

**Откройте:**
1. Страницу InfoBot
2. Нажмите F12 (Developer Tools)
3. Вкладка "Console"
4. Попробуйте включить Auto Bot

**Что должно быть в консоли:**
```
[BotsManager] 🤖 ИЗМЕНЕНИЕ ПЕРЕКЛЮЧАТЕЛЯ: true
[BotsManager] 🔒 Флаг data-user-changed установлен
[BotsManager] 📡 Отправка запроса на включение автобота...
[BotsManager] 📡 Ответ получен: 200
[BotsManager] ✅ Auto Bot включен и сохранен
[BotsManager] 🔓 Флаг data-user-changed снят после задержки
```

**Если видите ошибки:**
- ❌ `404 Not Found` → сервис bots.py не запущен
- ❌ `Connection refused` → сервис bots.py не доступен
- ❌ `success: false` → ошибка на бэкенде

---

### Шаг 2: Проверьте что bots.py запущен

```bash
# PowerShell:
netstat -ano | findstr "5001"

# Должно быть:
TCP    127.0.0.1:5001         0.0.0.0:0              LISTENING       12345
```

**Если порт не занят:**
```bash
# Запустите bots.py:
python bots.py
```

---

### Шаг 3: Проверьте элемент в HTML

**Откройте:**
1. Developer Tools (F12)
2. Вкладка "Elements"
3. Найдите: `<input type="checkbox" id="globalAutoBotToggle">`

**Проверьте:**
- ✅ Элемент существует
- ✅ Атрибут `data-initialized="true"` установлен
- ✅ При клике меняется `checked`

**Если элемент не найден:**
- Проверьте что вы на правильной вкладке ("🎮 Управление")
- Обновите страницу (F5)

---

### Шаг 4: Проверьте обработчик события

**В консоли браузера выполните:**
```javascript
const toggle = document.getElementById('globalAutoBotToggle');
console.log('Toggle element:', toggle);
console.log('Has data-initialized:', toggle?.hasAttribute('data-initialized'));
console.log('Checked:', toggle?.checked);
```

**Должно быть:**
```
Toggle element: <input type="checkbox" id="globalAutoBotToggle">
Has data-initialized: true
Checked: false
```

---

### Шаг 5: Проверьте API endpoint

**В консоли браузера выполните:**
```javascript
fetch('http://127.0.0.1:5001/api/bots/auto-bot', {
    method: 'POST',
    headers: {'Content-Type': 'application/json'},
    body: JSON.stringify({enabled: true})
})
.then(r => r.json())
.then(d => console.log('Response:', d));
```

**Должно быть:**
```
Response: {
  success: true,
  message: "Конфигурация Auto Bot обновлена и сохранена",
  config: {...}
}
```

---

## 🐛 ВОЗМОЖНЫЕ ПРОБЛЕМЫ

### Проблема 1: Переключатель не инициализирован

**Причина:** Функция `initializeGlobalAutoBotToggle()` не вызвалась

**Решение:**
```javascript
// В консоли браузера:
window.botsManager.initializeGlobalAutoBotToggle();
```

### Проблема 2: Сервис bots.py не запущен

**Причина:** Порт 5001 не слушает

**Решение:**
```bash
python bots.py
```

### Проблема 3: CORS ошибка

**Причина:** Браузер блокирует запросы

**Решение:** Проверьте что в `bots.py` есть CORS headers

### Проблема 4: Автообновление сразу выключает

**Причина:** Флаг `data-user-changed` не работает

**Решение:** Уже исправлено (задержка 5 секунд)

---

## 🔧 ВРЕМЕННОЕ РЕШЕНИЕ

### Если переключатель не работает, включите через API:

**В консоли браузера:**
```javascript
fetch('http://127.0.0.1:5001/api/bots/auto-bot', {
    method: 'POST',
    headers: {'Content-Type': 'application/json'},
    body: JSON.stringify({enabled: true})
})
.then(r => r.json())
.then(d => {
    console.log('Auto Bot включен:', d);
    alert('✅ Auto Bot включен!');
});
```

---

## 📝 ЧТО ПРОВЕРИТЬ

1. ✅ Сервис bots.py запущен (порт 5001)
2. ✅ Элемент `globalAutoBotToggle` существует
3. ✅ Обработчик инициализирован (`data-initialized="true"`)
4. ✅ API endpoint отвечает
5. ✅ Нет ошибок в консоли браузера
6. ✅ Нет ошибок в логах bots.py

---

## 🎯 ИТОГ

**Следуйте шагам диагностики выше!**

Если проблема не решается:
1. Скопируйте ошибки из консоли браузера
2. Скопируйте ошибки из logs/bots.log
3. Сообщите результаты проверок

**Я помогу исправить!** 🔧

---

**Версия:** 1.0  
**Дата:** 09.10.2025  
**Статус:** 🔍 Руководство по отладке
