# Настройка автоматического обучения AI

**Дата:** 2025-10-17  
**Статус:** ✅ Настроено для всех монет

---

## ✅ ТЕКУЩИЕ НАСТРОЙКИ

```python
# bot_engine/bot_config.py

class AIConfig:
    # Автоматическое обучение
    AI_AUTO_TRAIN_ENABLED = True  # ✅ Включено
    
    # Обновление данных
    AI_AUTO_UPDATE_DATA = True              # ✅ Включено
    AI_DATA_UPDATE_INTERVAL = 86400         # Каждые 24 часа
    AI_UPDATE_COINS_COUNT = 0               # ✅ ВСЕ монеты (не только топ-50)
    
    # Переобучение модели
    AI_AUTO_RETRAIN = True                  # ✅ Включено
    AI_RETRAIN_INTERVAL = 604800            # Каждые 7 дней
    AI_RETRAIN_HOUR = 3                     # В 3:00 ночи
    
    # Первичная настройка
    AI_AUTO_TRAIN_ON_STARTUP = True         # ✅ Автообучение при старте (если модель не найдена)
    AI_INITIAL_COINS_COUNT = 0              # ✅ ВСЕ монеты (при первичной настройке)
```

---

## 🔄 КАК ЭТО РАБОТАЕТ

### 1. **Ежедневное обновление данных** (каждые 24 часа)

**Что происходит:**
- Скачивает свежие свечи для **ВСЕХ 583 монет** с биржи
- Добавляет только **новые свечи** (последние 30 дней)
- Инкрементальное обновление - быстро (~5-10 минут)

**Логи:**
```
[AutoTrainer] 📥 Обновление исторических данных...
[AutoTrainer] Режим: ВСЕ монеты с биржи
[AutoTrainer] Запуск: python scripts/ai/collect_historical_data.py --all --days 30
[AutoTrainer] ✅ Данные успешно обновлены
[AutoTrainer] 📊 Добавлено ~2332 новых свечей (4 свечи × 583 монеты)
```

**Зачем все монеты, а не топ-50:**
- ✅ Pump/dump чаще на низколиквидных монетах (не в топе)
- ✅ Разные паттерны у разных типов монет
- ✅ Модель не "забывает" редкие монеты
- ✅ Инкрементальное обновление быстрое (только новые свечи)

---

### 2. **Еженедельное переобучение** (каждые 7 дней в 3:00)

**Что происходит:**
- Переобучает модель на **всех** собранных данных (старые + новые)
- Использует все исторические данные из `data/ai/historical/`
- Сохраняет новую модель в `data/ai/models/`
- **Горячая перезагрузка** - модель обновляется без перезапуска бота

**Логи:**
```
[AutoTrainer] 🧠 Начинаем переобучение модели...
[AutoTrainer] Запуск: python scripts/ai/train_anomaly_on_real_data.py
[AutoTrainer] 📊 Загружено 15000 примеров для обучения
[AutoTrainer] ✅ Модель обучена (аномалий обнаружено: 1500)
[AutoTrainer] 🔄 Горячая перезагрузка модели...
[AutoTrainer] ✅ Модель перезагружена без перезапуска бота
```

**Почему в 3:00:**
- Минимальная нагрузка на систему
- Меньше активных сделок
- Не мешает дневной торговле

---

### 3. **Первичная настройка** (при старте, если модель не найдена)

**Что происходит:**
- Автоматически собирает данные для **ВСЕХ монет**
- Загружает полную историю (730 дней = 2 года)
- Обучает модель
- Сохраняет и загружает

**Логи:**
```
[AutoTrainer] 🆕 Модель не найдена, выполняем первичную настройку...
[AutoTrainer] Режим: ВСЕ монеты с биржи
[AutoTrainer] 📥 Собираем данные для всех 583 монет (730 дней)...
[AutoTrainer] 🧠 Обучаем модель на собранных данных...
[AutoTrainer] ✅ Первичная настройка завершена
```

---

## 📊 СТАТИСТИКА ОБНОВЛЕНИЙ

### Ежедневное обновление (инкрементальное):
- **Монет:** 583 (все монеты с биржи)
- **Свечей на монету:** ~4 (1 день на 6H таймфрейме)
- **Всего свечей:** ~2332
- **Время:** ~5-10 минут
- **Объём данных:** ~500 KB

### Еженедельное переобучение:
- **Примеров для обучения:** ~15000+ (зависит от истории)
- **Монет:** 583
- **Время обучения:** ~2-5 минут
- **Размер модели:** ~800 KB (anomaly_detector.pkl)
- **Размер scaler:** ~1 KB (anomaly_scaler.pkl)

### Первичная настройка (разовая):
- **Монет:** 583
- **Свечей на монету:** ~2920 (730 дней × 4 свечи)
- **Всего свечей:** ~1,700,000
- **Время:** ~30-60 минут
- **Объём данных:** ~350 MB

---

## 🎯 ПРЕИМУЩЕСТВА ТЕКУЩИХ НАСТРОЕК

### ✅ Все монеты, не только топ-50:
1. **Лучшая точность:** Модель знает паттерны всех типов монет
2. **Обнаружение pump/dump:** Аномалии чаще на низколиквидных монетах
3. **Нет деградации:** Модель не "забывает" редкие монеты
4. **Быстрое обновление:** Инкрементальное добавление только новых свечей

### ✅ Автоматизация:
1. **Нет ручной работы:** Данные обновляются автоматически
2. **Актуальность:** Модель всегда обучена на свежих данных
3. **Горячая перезагрузка:** Нет простоя бота
4. **Расписание:** Обновления в ночное время (минимум нагрузки)

### ✅ Гибкость:
1. **Настраиваемые интервалы:** Можно изменить частоту обновлений
2. **Выборочное отключение:** Можно отключить обновление или переобучение отдельно
3. **Первичная настройка:** Автоматическая настройка при первом запуске

---

## ⚙️ НАСТРОЙКА ПОД СЕБЯ

### Изменить частоту обновления данных:
```python
AI_DATA_UPDATE_INTERVAL = 43200  # 12 часов (вместо 24)
```

### Изменить частоту переобучения:
```python
AI_RETRAIN_INTERVAL = 259200  # 3 дня (вместо 7)
```

### Изменить время переобучения:
```python
AI_RETRAIN_HOUR = 5  # 5:00 утра (вместо 3:00)
```

### Ограничить количество монет (не рекомендуется):
```python
AI_UPDATE_COINS_COUNT = 100  # Топ-100 вместо всех
# Причина ограничения: экономия ресурсов, но ухудшение качества
```

### Отключить автообучение:
```python
AI_AUTO_TRAIN_ENABLED = False  # Полностью отключить
```

### Отключить только обновление данных:
```python
AI_AUTO_UPDATE_DATA = False  # Только переобучение на существующих данных
```

### Отключить только переобучение:
```python
AI_AUTO_RETRAIN = False  # Только обновление данных, без переобучения
```

---

## 🔍 МОНИТОРИНГ

### Проверить статус автообучения:
```bash
# Проверить логи
tail -f logs/bots.log | grep AutoTrainer

# Проверить последнее обновление
ls -lh data/ai/historical/*.csv

# Проверить обученную модель
python scripts/test_ai_detector_status.py
```

### Проверить следующее обновление:
```python
# Смотреть в логах при старте бота:
[AutoTrainer] ⏰ Следующее обновление данных: через 23 часа 45 минут
[AutoTrainer] ⏰ Следующее переобучение: 24.10.2025 03:00
```

---

## 📝 РУЧНОЕ УПРАВЛЕНИЕ

### Ручное обновление данных:
```bash
# Обновить все монеты (последние 30 дней)
python scripts/ai/collect_historical_data.py --all --days 30

# Обновить топ-50 (последние 7 дней)
python scripts/ai/collect_historical_data.py --limit 50 --days 7
```

### Ручное переобучение:
```bash
# Переобучить модель на всех данных
python scripts/ai/train_anomaly_on_real_data.py
```

### Проверить статус модели:
```bash
# Детальная информация о модели
python scripts/test_ai_detector_status.py
```

---

## ⚠️ ВАЖНО

1. **Первый запуск:** Если модель не найдена, автоматически запустится первичная настройка (30-60 минут)
2. **Горячая перезагрузка:** После переобучения модель перезагружается автоматически без перезапуска бота
3. **Нагрузка на биржу:** Инкрементальное обновление щадящее (~2332 запроса в день = 1 запрос каждые 37 секунд)
4. **Дисковое пространство:** ~350 MB для всех исторических данных + ~1 MB для модели
5. **Время обучения:** 2-5 минут для переобучения, не влияет на торговлю

---

## 🎉 ИТОГО

**Текущая конфигурация оптимальна для:**
- ✅ Максимальной точности обнаружения аномалий
- ✅ Работы со всеми монетами на бирже
- ✅ Автоматического поддержания актуальности
- ✅ Минимальной нагрузки на систему

**Модель будет:**
- 🔄 Обновлять данные **каждый день** (все монеты)
- 🧠 Переобучаться **каждую неделю** (в 3:00)
- 🚀 Работать **без перезапуска** бота (горячая перезагрузка)
- 🎯 Обнаруживать аномалии на **всех типах монет** (не только топ-50)

