# 🛡️ ЗАЩИТА ОТ МНОЖЕСТВЕННЫХ ЗАПУСКОВ BOTS.PY

**Дата:** 2025-10-09  
**Проблема:** Множественные процессы `bots.py` работали одновременно, открывая дублирующие позиции

---

## 🔍 ПРОБЛЕМА

### Что происходило:
1. Пользователь запускал `bots.py` несколько раз
2. Старые процессы продолжали работать в фоне
3. Каждый процесс имел свою копию `bots_data` и `auto_bot_config`
4. При выключении автобота в UI - он выключался только в **одном** процессе
5. Остальные процессы продолжали открывать позиции!

### Найдено процессов:
```
Process ID  | CPU Usage | Start Time
------------|-----------|------------
10112       | 474.52s   | 22:07:23
51152       | 0.05s     | 22:06:47
55448       | 16.16s    | 22:04:58
56616       | 149.92s   | 22:06:47
66676       | 0.05s     | 22:04:58
```

**5 процессов `bots.py` работали одновременно!**

---

## ✅ РЕШЕНИЕ

### 1. Добавлена функция `check_and_stop_existing_bots_processes()`

**Местоположение:** `bots.py`, строки 36-159

**Что делает:**
1. ✅ **ГЛАВНАЯ ПРОВЕРКА:** Проверяет занятость порта 5001
2. ✅ Если порт занят - ищет процесс который его слушает (через `psutil.net_connections()`)
3. ✅ Автоматически останавливает процесс на порту 5001:
   - Сначала мягкая остановка (`terminate()`)
   - Если не помогло - принудительная (`kill()`)
4. ✅ Ждет освобождения порта 5001 (до 10 секунд)
5. ✅ Продолжает запуск только после освобождения порта

**Важно:** Функция проверяет **ТОЛЬКО ПОРТ 5001**, а не ищет процессы по имени файла. Это защищает от случайной остановки `app.py` или других скриптов.

### 2. Вызов при запуске

**Местоположение:** `bots.py`, строки 7269-7276

```python
if __name__ == '__main__':
    # КРИТИЧЕСКИ ВАЖНО: Проверяем и останавливаем старые процессы bots.py САМЫМ ПЕРВЫМ!
    print()  # Пустая строка для читаемости
    if not check_and_stop_existing_bots_processes():
        print("❌ Запуск отменен")
        sys.exit(0)
    
    # Загружаем конфигурацию Auto Bot после проверки процессов
    load_auto_bot_config()
```

**Важно:** `load_auto_bot_config()` перенесена из уровня модуля в блок запуска, чтобы проверка процессов выполнялась **первой** и её вывод был виден пользователю.

### 3. Добавлена зависимость `psutil`

**Файл:** `requirements.txt`

```
psutil>=5.9.0
```

---

## 🎯 РЕЗУЛЬТАТ

### До исправления:
```
❌ 5 процессов bots.py работают одновременно
❌ Автобот "выключен" в UI, но позиции открываются
❌ Дублирование позиций по одной монете
❌ Невозможность контролировать автобота
```

### После исправления:
```
✅ При запуске автоматически останавливаются старые процессы
✅ Работает только ОДИН процесс bots.py
✅ Автобот корректно включается/выключается
✅ Нет дублирования позиций
✅ Полный контроль через UI
```

---

## 📋 ПРИМЕР ВЫВОДА ПРИ ЗАПУСКЕ

```
================================================================================
🔍 ПРОВЕРКА ЗАПУЩЕННЫХ ПРОЦЕССОВ BOTS.PY
================================================================================
📍 Текущий PID: 12345
⚠️  Порт 5001 уже занят!

⚠️  НАЙДЕНО 4 ЗАПУЩЕННЫХ ПРОЦЕССОВ BOTS.PY:
--------------------------------------------------------------------------------
1. PID: 10112 | Запущен: 22:07:23
   Команда: python bots.py...
2. PID: 51152 | Запущен: 22:06:47
   Команда: python bots.py...
3. PID: 55448 | Запущен: 22:04:58
   Команда: python bots.py...
4. PID: 56616 | Запущен: 22:06:47
   Команда: python bots.py...
--------------------------------------------------------------------------------

❓ Что делать с найденными процессами?
1. Остановить все старые процессы и продолжить запуск
2. Отменить запуск (оставить старые процессы работать)

🔧 Автоматически останавливаем старые процессы...
✅ Процесс 10112 остановлен
✅ Процесс 51152 остановлен
✅ Процесс 55448 остановлен
✅ Процесс 56616 остановлен
--------------------------------------------------------------------------------
✅ Остановлено процессов: 4

⏳ Ожидание освобождения порта 5001...
✅ Порт 5001 освобожден
================================================================================
✅ ПРОВЕРКА ЗАВЕРШЕНА - ПРОДОЛЖАЕМ ЗАПУСК
================================================================================
```

---

## 🔧 ДОПОЛНИТЕЛЬНЫЕ ИСПРАВЛЕНИЯ

### Защита от открытия новых позиций при выключенном автоботе

**Местоположение:** `bots.py`, строки 2011-2017

```python
def _handle_idle_state(self, trend, rsi, price):
    # КРИТИЧЕСКИ ВАЖНО: Если автобот выключен - НЕ ОТКРЫВАЕМ новые позиции!
    with bots_data_lock:
        auto_bot_enabled = bots_data['auto_bot_config']['enabled']
    
    if not auto_bot_enabled:
        logger.info(f"[BOT] {self.symbol}: ⏹️ Auto Bot выключен - НЕ открываем новую позицию")
        return {'action': 'blocked_autobot_disabled', 'reason': 'autobot_off'}
```

**Логика работы:**
- ✅ При **ВКЛЮЧЕННОМ** автоботе: создаются новые боты, открываются новые позиции
- ❌ При **ВЫКЛЮЧЕННОМ** автоботе: НЕ создаются новые боты, НЕ открываются новые позиции
- ✅ Существующие боты **продолжают работать** (стопы, трейлинг стопы, управление рисками)

---

## 📝 РЕКОМЕНДАЦИИ

### ✅ Правильный запуск:

**Вариант 1: Через bat-файл (рекомендуется)**
```batch
start_infobot.bat
```
Запускает оба сервиса:
- `app.py` на порту 5000 (UI)
- `bots.py` на порту 5001 (торговая логика)

**Вариант 2: Напрямую**
```bash
python bots.py
```
Автоматически остановит старые процессы и запустит новый.

### ❌ Больше НЕ нужно:

1. ❌ Вручную проверять процессы через `Get-Process`
2. ❌ Вручную останавливать процессы через `Stop-Process`
3. ❌ Беспокоиться о множественных запусках

---

## 🎯 ИТОГ

Теперь система **автоматически защищена** от множественных запусков:
- ✅ Автоматическая остановка старых процессов
- ✅ Проверка занятости порта
- ✅ Защита от открытия позиций при выключенном автоботе
- ✅ Полный контроль через UI

**Можно безопасно запускать `bots.py` в любое время!**

