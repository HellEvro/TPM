# AI Модули - Управление через UI

**Дата:** 2025-10-17  
**Статус:** ✅ Реализовано

---

## 🎯 НАЗНАЧЕНИЕ

UI панель для управления всеми настройками AI модулей через веб-интерфейс.

**Доступ:** Только при наличии валидной лицензии

---

## 📍 РАСПОЛОЖЕНИЕ В UI

**Страница:** Боты → Конфигурация  
**Блок:** "🤖 AI Модули (Premium)" (в конце секции конфигурации)

**Видимость:** Блок показывается автоматически если:
- ✅ AI_ENABLED = True в bot_config.py
- ✅ Лицензия валидна (license.lic существует)

---

## ⚙️ НАСТРОЙКИ В UI

### 1. **Мастер-переключатель**
```
🤖 Включить AI модули: [ON/OFF]
```
- Отключает/включает все AI функции разом
- Эквивалент: `AIConfig.AI_ENABLED`

---

### 2. **Anomaly Detection (Обнаружение аномалий)**

#### Включение:
```
Обнаружение аномалий: [ON/OFF]
```
- Блокирует входы при обнаружении PUMP/DUMP
- Эквивалент: `AIConfig.AI_ANOMALY_DETECTION_ENABLED`
- **⚠️ ВАЖНО:** Включается ТОЛЬКО при входе в позицию в `_enter_position()` внутри ExitScam фильтра, а не для всех монет постоянно

#### Порог блокировки:
```
⚠️ Порог блокировки: [0.7] %
```
- Диапазон: 0.5-0.95 (50%-95%)
- По умолчанию: 0.7 (70%)
- Эквивалент: `AIConfig.AI_ANOMALY_BLOCK_THRESHOLD`

#### Логирование:
```
Логировать аномалии: [ON/OFF]
```
- Записывать обнаруженные аномалии в логи
- Эквивалент: `AIConfig.AI_LOG_ANOMALIES`

---

### 3. **Risk Management (Управление рисками)**

#### Включение:
```
Умное управление рисками: [ON/OFF]
```
- Адаптивные SL/TP и размер позиции
- Эквивалент: `AIConfig.AI_RISK_MANAGEMENT_ENABLED`
- **⚠️ ВАЖНО:** Включается ТОЛЬКО при входе в позицию в `_enter_position()` для адаптации размера позиции и стоп-лосса, а не для всех монет постоянно

#### Интервал обновления:
```
⏰ Интервал обновления: [300] сек
```
- Диапазон: 60-3600 сек (1-60 минут)
- По умолчанию: 300 сек (5 минут)
- Эквивалент: `AIConfig.AI_RISK_UPDATE_INTERVAL`

---

### 4. **ML модель параметров (Parameter Quality)**

#### Включение оценки качества параметров:
```
Оценка качества параметров (Parameter Quality): [ON/OFF]
```
- ML модель для предсказания качества торговых параметров (AIContinuousLearning, ParameterQualityPredictor)
- **Выключить**, если тормозит систему или торговлю
- Эквивалент: `AIConfig.AI_PARAMETER_QUALITY_ENABLED`
- **Требует перезапуска** для полного отключения

---

### 5. **Auto Training (Автообучение)**

#### Включение автообучения:
```
Автоматическое обучение: [ON/OFF]
```
- Включает автоматическое обновление и переобучение
- Эквивалент: `AIConfig.AI_AUTO_TRAIN_ENABLED`

#### Обновление данных:
```
Обновление данных: [ON/OFF]
```
- Ежедневное обновление исторических данных
- Эквивалент: `AIConfig.AI_AUTO_UPDATE_DATA`

#### Интервал обновления данных:
```
📥 Интервал обновления данных: [86400] часов
```
- Диапазон: 3600-604800 сек (1 час - 7 дней)
- По умолчанию: 86400 сек (24 часа)
- Эквивалент: `AIConfig.AI_DATA_UPDATE_INTERVAL`

#### Переобучение:
```
Автоматическое переобучение: [ON/OFF]
```
- Еженедельное переобучение модели
- Эквивалент: `AIConfig.AI_AUTO_RETRAIN`

#### Интервал переобучения:
```
🧠 Интервал переобучения: [604800] дней
```
- Диапазон: 86400-2592000 сек (1-30 дней)
- По умолчанию: 604800 сек (7 дней)
- Эквивалент: `AIConfig.AI_RETRAIN_INTERVAL`

#### Время переобучения:
```
⏰ Время переобучения: [3] час
```
- Диапазон: 0-23 (час дня)
- По умолчанию: 3 (3:00 AM)
- Эквивалент: `AIConfig.AI_RETRAIN_HOUR`

---

### 5. **Логирование AI**

#### Логировать предсказания:
```
Логировать предсказания: [ON/OFF]
```
- Записывать все AI решения в логи
- Эквивалент: `AIConfig.AI_LOG_PREDICTIONS`

#### Логировать аномалии:
```
Логировать аномалии: [ON/OFF]
```
- Записывать обнаруженные аномалии
- Эквивалент: `AIConfig.AI_LOG_ANOMALIES`

---

## 💾 СОХРАНЕНИЕ

### Кнопка сохранения:
```
💾 Сохранить AI настройки
```

**Что происходит:**
1. Данные из формы собираются
2. Отправляются на `/api/ai/config` (POST)
3. Обновляется `bot_engine/bot_config.py`
4. Модуль `bot_config` перезагружается
5. Показывается уведомление об успехе/ошибке

**Важно:** 
- ⚠️ Auto Trainer требует **перезапуска бота** для применения изменений
- ✅ Остальные настройки применяются сразу (Hot Reload)

---

## 🔐 ПРОВЕРКА ЛИЦЕНЗИИ

### Автоматическая проверка:

При загрузке страницы конфигурации:
1. Отправляется запрос на `/api/ai/config` (GET)
2. Проверяется `license.valid`
3. Если `true` → блок AI показывается
4. Если `false` → блок AI скрывается

### Badge лицензии:

**При валидной лицензии:**
```
✅ Лицензия активна: developer
```

**При невалидной:**
```
❌ Лицензия недействительна
```

---

## 📡 API ENDPOINTS

### GET /api/ai/config
**Получение AI конфигурации**

**Response:**
```json
{
  "success": true,
  "license": {
    "valid": true,
    "type": "developer",
    "expires_at": "9999-12-31",
    "features": {
      "anomaly_detection": true,
      "risk_management": true,
      "lstm_predictor": false,
      "pattern_recognition": false
    }
  },
  "config": {
    "ai_enabled": true,
    "anomaly_detection_enabled": true,
    "anomaly_block_threshold": 0.7,
    "risk_management_enabled": true,
    "auto_train_enabled": true,
    ...
  }
}
```

---

### POST /api/ai/config
**Сохранение AI конфигурации**

**Request:**
```json
{
  "ai_enabled": true,
  "anomaly_detection_enabled": true,
  "anomaly_block_threshold": 0.7,
  "risk_management_enabled": true,
  "auto_train_enabled": true,
  ...
}
```

**Response:**
```json
{
  "success": true,
  "message": "AI конфигурация сохранена"
}
```

**Ошибка (403):**
```json
{
  "success": false,
  "error": "Недействительная лицензия"
}
```

---

## 🔧 ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Файлы:

**Backend:**
- `bot_engine/api/endpoints_ai.py` - API endpoints для AI конфигурации

**Frontend:**
- `templates/pages/bots.html` - HTML блок AI настроек (строки 1541-1750)
- `static/js/managers/ai_config_manager.js` - JS логика управления
- `static/js/config.js` - Локализация (русский + английский)

### Классы:

**JavaScript:**
```javascript
class AIConfigManager {
    async loadAIConfig()      // Загрузка конфигурации
    populateForm()            // Заполнение формы
    async saveAIConfig()      // Сохранение конфигурации
    showAIConfigSection()     // Показать блок
    hideAIConfigSection()     // Скрыть блок
    updateLicenseBadge()      // Обновить badge лицензии
}
```

---

## 📝 ПРИМЕР ИСПОЛЬЗОВАНИЯ

### 1. Открыть страницу конфигурации:
- Перейти в "Боты" → вкладка "Конфигурация"
- Прокрутить вниз до блока "🤖 AI Модули (Premium)"

### 2. Изменить настройки:
- Включить/выключить нужные модули
- Настроить пороги и интервалы
- Изменить параметры автообучения

### 3. Сохранить:
- Нажать "💾 Сохранить AI настройки"
- Дождаться уведомления "✅ AI конфигурация сохранена"

### 4. Применить (опционально):
- Для Auto Trainer: перезапустить бот
- Для остальных: Hot Reload в UI

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

### Если лицензия недействительна:
- Блок AI **не отображается** в UI
- API возвращает 403 при попытке сохранения
- Нужно активировать лицензию

### Если нужно активировать лицензию:
```bash
# Developer лицензия (для разработки)
echo "DEVELOPER_LICENSE_DO_NOT_COMMIT" > license.lic

# Или premium лицензия
python scripts/activate_premium.py --key license_key.json
```

### Горячая перезагрузка:
- Изменения Anomaly Detection применяются **сразу**
- Изменения Risk Management применяются **сразу**
- Изменения Auto Trainer требуют **перезапуска бота**

---

## ⚡ КОГДА AI МОДУЛИ ВКЛЮЧАЮТСЯ

**Все AI модули включаются ТОЛЬКО при входе в позицию** в `bot_engine/trading_bot.py::_enter_position()`:

1. **AI Anomaly Detection** - включается внутри ExitScam фильтра через `apply_entry_filters()`
2. **AI Optimal Entry Detection** - вызывается после всех фильтров, может отложить вход
3. **AI Risk Management** - адаптирует размер позиции и стоп-лосс при входе

**Порядок проверки в `_enter_position()`:**
```
1. Проверка делистинга
2. Проверка существующих позиций
3. ✅ apply_entry_filters() - все фильтры:
   - Global switches
   - Scope (whitelist/blacklist)
   - Trend
   - Maturity
   - RSI Time Filter
   - ExitScam
     └─ 🤖 AI Anomaly Detection (если включен)
4. 🤖 AI Optimal Entry Detection (если включен)
5. 🤖 AI Risk Management - адаптация размера позиции
6. Открытие позиции на бирже
7. 🤖 AI Risk Management - адаптация стоп-лосса
```

**Оптимизация:** AI модули НЕ проверяются для всех монет постоянно, только при реальном входе в позицию. Это значительно улучшает производительность.

---

## ✅ ИТОГО

**Реализовано:**
- ✅ HTML форма с 12 настройками
- ✅ API endpoints (GET/POST)
- ✅ JS логика загрузки и сохранения
- ✅ Проверка лицензии
- ✅ Локализация (русский + английский)
- ✅ Badge статуса лицензии

**Работает:**
- ✅ Автоматическое показывание/скрытие блока
- ✅ Сохранение в bot_config.py
- ✅ Hot Reload для большинства настроек
- ✅ Уведомления об успехе/ошибке
- ✅ AI модули включаются только при входе в позицию (оптимизация производительности)

**Готово к использованию!** 🚀

