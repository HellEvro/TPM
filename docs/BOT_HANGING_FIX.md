# Исправление проблемы зависания bots.py

## Проблема
Система `bots.py` зависала через некоторое время после загрузки, что приводило к остановке торговых ботов.

## Причины зависания
1. **Отсутствие таймаутов** для API вызовов к бирже
2. **Блокирующие операции** в основном цикле
3. **Отсутствие мониторинга** состояния системы
4. **Недостаточная обработка ошибок** и восстановление после сбоев

## Внесенные исправления

### 1. Добавлены таймауты для API вызовов
- **Файл**: `exchanges/base_exchange.py`
- **Изменения**: Добавлен декоратор `@with_timeout()` для ограничения времени выполнения API вызовов
- **Таймауты**:
  - `get_ticker()`: 10 секунд
  - `get_chart_data()`: 30 секунд  
  - `place_order()`: 15 секунд

### 2. Улучшен основной цикл обработки
- **Файл**: `bots.py`
- **Изменения**:
  - Добавлен heartbeat механизм (каждую минуту)
  - Добавлен таймаут для обработки ботов (2 минуты)
  - Добавлен счетчик ошибок с автоматическим восстановлением
  - Улучшена обработка исключений

### 3. Добавлен мониторинг состояния системы
- **Функция**: `log_system_health()`
- **Проверяет**:
  - Состояние соединения с биржей
  - Количество активных ботов
  - Количество RSI данных
  - Общее состояние системы

### 4. Добавлено автоматическое восстановление
- **Функция**: `attempt_system_recovery()`
- **Восстанавливает**:
  - Соединение с биржей
  - Состояние данных ботов
  - RSI данные

### 5. Улучшена обработка ошибок
- Добавлены детальные traceback'и
- Добавлена защита от зацикливания
- Добавлено логирование времени выполнения операций

## Новые возможности

### Мониторинг системы
Создан скрипт `monitor_bots.py` для отслеживания состояния системы:
```bash
python monitor_bots.py
```

### Heartbeat логи
Система теперь выводит heartbeat каждую минуту:
```
[HEARTBEAT] 💓 Система работает нормально (ошибок: 0)
[HEALTH] 🏥 Система: Exchange=OK, Bots=5/10, RSI=150
```

## Рекомендации по использованию

1. **Мониторинг**: Запускайте `monitor_bots.py` для отслеживания состояния
2. **Логи**: Регулярно проверяйте логи на наличие ошибок
3. **Восстановление**: Система автоматически пытается восстановиться после сбоев
4. **Таймауты**: Все API вызовы теперь имеют таймауты для предотвращения зависания

## Тестирование

Для проверки исправлений:
1. Запустите `bots.py`
2. Запустите `monitor_bots.py` в отдельном терминале
3. Наблюдайте за heartbeat сообщениями
4. Проверьте, что система не зависает

## Дополнительные улучшения

- Добавлена защита от rate limiting
- Улучшена обработка сетевых ошибок
- Добавлено логирование производительности
- Улучшена стабильность работы с блокировками
