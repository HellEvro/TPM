# AI Инициализация при запуске

## Проблема (до исправления)

**Проблема**: AI модули инициализировались только при первой попытке их использования (в `check_exit_scam_filter`). Это приводило к:
- Ненужным попыткам загрузки модулей, если лицензия отсутствует
- Запуску Auto Trainer без проверки доступности AI
- Трате ресурсов на сбор данных и обучение, даже если AI недоступен

## Решение

### 1. Ранняя инициализация AI Manager

AI Manager теперь инициализируется **сразу при запуске бота** (в `bots.py`), до запуска Auto Trainer:

```python
# bots.py
if AIConfig.AI_ENABLED:
    logger.info("🤖 Инициализация AI модулей...")
    from bot_engine.ai.ai_manager import get_ai_manager
    ai_manager = get_ai_manager()
    
    # Только если AI доступен, запускаем Auto Trainer
    if ai_manager.is_available() and AIConfig.AI_AUTO_TRAIN_ENABLED:
        from bot_engine.ai.auto_trainer import start_auto_trainer
        start_auto_trainer()
        logger.info("🤖 AI Auto Trainer запущен")
    elif not ai_manager.is_available():
        logger.warning("⚠️ AI модули не загружены (проверьте лицензию)")
```

### 2. Проверка лицензии при инициализации

При создании `AIManager` происходит:

1. **Проверка `AI_ENABLED`** в конфигурации
2. **Проверка наличия premium модулей** (файлы `.pyd/.so`)
3. **Проверка лицензии** (`license.lic`)
4. **Загрузка только доступных модулей** согласно лицензии

Если любая из проверок не пройдена:
- Модули не загружаются
- Auto Trainer не запускается
- Выводится информативное сообщение

### 3. Кэширование результата проверки

Метод `is_available()` теперь кэширует результат:

```python
def is_available(self) -> bool:
    if self._availability_cache is None:
        self._availability_cache = (
            self.premium_loader.premium_available and 
            self.premium_loader.license_valid and
            any([self.anomaly_detector is not None, ...])
        )
    return self._availability_cache
```

Это значительно ускоряет повторные вызовы (используется в фильтрах для каждой монеты).

### 4. Быстрая проверка в фильтрах

В `check_exit_scam_filter` теперь используется быстрая проверка:

```python
ai_manager = get_ai_manager()

# Быстрая проверка: если AI недоступен, пропускаем
if not ai_manager.is_available():
    pass  # Не логируем, чтобы не спамить
elif ai_manager.anomaly_detector:
    # Используем AI для анализа
    ...
```

## Преимущества

### ✅ Производительность
- **Ранняя проверка лицензии**: один раз при старте, а не при каждом сигнале
- **Кэширование результата**: `is_available()` работает мгновенно
- **Нет ненужных загрузок**: Auto Trainer не запускается, если AI недоступен

### ✅ Экономия ресурсов
- **Нет сбора данных** без лицензии
- **Нет обучения моделей** без лицензии
- **Нет попыток загрузить модули** при каждом сигнале

### ✅ Ясная диагностика
- **Информативные логи** при старте:
  ```
  [AI] ⚠️ Лицензия недействительна или отсутствует
  [AI] 💡 Активируйте лицензию: python scripts/activate_premium.py
  [AI] ⚠️ AI функции будут отключены до активации лицензии
  ```
- **Один раз при старте**, а не при каждом использовании

### ✅ Правильная последовательность
1. Проверка лицензии
2. Загрузка AI модулей (если лицензия OK)
3. Запуск Auto Trainer (если AI доступен)
4. Использование в фильтрах (быстрая проверка)

## Тестирование

Используйте скрипт для проверки инициализации:

```bash
python scripts/test_ai_initialization.py
```

Тест покажет:
- Настройки AI (включен/выключен)
- Статус лицензии (валидна/невалидна)
- Загруженные модули (Anomaly Detector, LSTM, Pattern, Risk)
- Доступность AI для использования
- Производительность кэширования

## Активация AI

### Для разработки (Developer License)

1. Создайте файл `license.lic` в корне проекта:
   ```
   DEVELOPER_LICENSE_DO_NOT_COMMIT
   ```

2. Установите `AI_ENABLED = True` в `bot_engine/bot_config.py`:
   ```python
   class AIConfig:
       AI_ENABLED = True
       AI_ANOMALY_DETECTION_ENABLED = True
       # ...
   ```

3. Перезапустите бот:
   ```bash
   python bots.py
   ```

### Для продакшн (Paid License)

1. Приобретите лицензию
2. Получите файл активации (`license_key.json`)
3. Активируйте:
   ```bash
   python scripts/activate_premium.py --key license_key.json
   ```
4. Перезапустите бот

## Отключение AI

Чтобы полностью отключить AI функции:

1. Установите `AI_ENABLED = False` в `bot_engine/bot_config.py`
2. Перезапустите бот

Это предотвратит любые попытки загрузки AI модулей и запуска Auto Trainer.

## Структура инициализации

```
bots.py (main)
├── Проверка AI_ENABLED
├── Инициализация AIManager
│   ├── Проверка premium модулей
│   ├── Проверка лицензии
│   ├── Загрузка Anomaly Detector
│   ├── Загрузка LSTM Predictor
│   ├── Загрузка Pattern Detector
│   └── Загрузка Risk Manager
├── Проверка ai_manager.is_available()
│   ├── Если TRUE  → Запуск Auto Trainer (если включен)
│   └── Если FALSE → Пропуск Auto Trainer
└── Запуск основного сервиса

filters.py (usage)
├── Получение get_ai_manager()
├── Быстрая проверка is_available()
│   ├── Если TRUE  → Использование AI для анализа
│   └── Если FALSE → Пропуск AI (без логов)
└── Продолжение стандартной логики фильтров
```

## Логи при старте

### С валидной лицензией:
```
[AI] 🎫 Лицензия: developer
[AI] 📅 Действительна до: 9999-12-31
[AI] ✅ Anomaly Detector загружен
[AI] 🎉 Загружено модулей: 1/4
🤖 AI модули активны (автообучение отключено)
```

### Без лицензии:
```
[AI] ⚠️ Лицензия недействительна или отсутствует
[AI] 💡 Активируйте лицензию: python scripts/activate_premium.py
[AI] ⚠️ AI функции будут отключены до активации лицензии
⚠️ AI модули не загружены (проверьте лицензию)
```

### AI отключен:
```
[AI] ℹ️ ИИ модули отключены в конфигурации
ℹ️ AI модули отключены в конфигурации
```

