# 📊 Pattern Recognition Module

## Обзор

**Pattern Recognition** - модуль для автоматического распознавания классических паттернов технического анализа на графиках криптовалют.

## Архитектура

### Подход к распознаванию

Модуль использует **гибридный подход**:
1. **Алгоритмический анализ** - геометрическое распознавание паттернов
2. **Статистический анализ** - оценка вероятности паттернов
3. **Machine Learning** - улучшение точности (RandomForest)

### Основной класс: `PatternDetector`

```python
from bot_engine.ai.pattern_detector import PatternDetector

detector = PatternDetector()
result = detector.detect_patterns(candles, current_price)
```

## Распознаваемые паттерны

### 1. Разворотные паттерны

#### Head & Shoulders (Голова и плечи)
- **Тип**: Медвежий разворот
- **Структура**: Левое плечо - Голова (выше) - Правое плечо
- **Условие**: Голова выше обоих плеч, плечи на одном уровне (±5%)
- **Сигнал**: `BEARISH`

#### Double Top (Двойная вершина)
- **Тип**: Медвежий разворот
- **Структура**: Две вершины на одном уровне (±2%)
- **Сигнал**: `BEARISH`

#### Double Bottom (Двойное дно)
- **Тип**: Бычий разворот
- **Структура**: Два дна на одном уровне (±2%)
- **Сигнал**: `BULLISH`

### 2. Паттерны продолжения

#### Symmetrical Triangle (Симметричный треугольник)
- **Структура**: Сходящиеся линии тренда (верхняя ↓, нижняя ↑)
- **Сигнал**: `NEUTRAL` (ждем пробоя)

#### Ascending Triangle (Восходящий треугольник)
- **Структура**: Горизонтальное сопротивление + растущая поддержка
- **Сигнал**: `BULLISH`

#### Descending Triangle (Нисходящий треугольник)
- **Структура**: Горизонтальная поддержка + падающее сопротивление
- **Сигнал**: `BEARISH`

### 3. Пробои уровней

#### Resistance Breakout (Пробой сопротивления)
- **Условие**: Цена > сопротивление + 0.5%
- **Сигнал**: `BULLISH`

#### Support Breakdown (Пробой поддержки)
- **Условие**: Цена < поддержка - 0.5%
- **Сигнал**: `BEARISH`

### 4. Свечные паттерны

#### Hammer (Молот)
- **Структура**: Длинная нижняя тень (> 2x тело), короткая верхняя
- **Сигнал**: `BULLISH`

#### Shooting Star (Падающая звезда)
- **Структура**: Длинная верхняя тень (> 2x тело), короткая нижняя
- **Сигнал**: `BEARISH`

#### Bullish Engulfing (Бычье поглощение)
- **Структура**: Бычья свеча поглощает предыдущую медвежью
- **Сигнал**: `BULLISH`

#### Bearish Engulfing (Медвежье поглощение)
- **Структура**: Медвежья свеча поглощает предыдущую бычью
- **Сигнал**: `BEARISH`

#### Doji (Доджи)
- **Структура**: Тело < 10% от диапазона свечи
- **Сигнал**: `NEUTRAL` (неопределенность)

## Параметры работы

### Конфигурация

```python
# bot_engine/bot_config.py

class AIConfig:
    AI_PATTERN_ENABLED = True
    AI_PATTERN_MODEL_PATH = 'data/ai/models/pattern_detector.pkl'
    AI_PATTERN_SCALER_PATH = 'data/ai/models/pattern_scaler.pkl'
    AI_PATTERN_WEIGHT = 1.2  # Вес в голосовании
    AI_PATTERN_MIN_CONFIDENCE = 0.6  # Минимальная уверенность (60%)
```

### Параметры анализа

- **Lookback period**: 100 свечей (25 дней на 6H)
- **Min pattern length**: 10 свечей
- **Confidence threshold**: 0.6 (60%)

## Интеграция с ботом

### Использование в фильтрах

```python
from bots_modules.filters import get_pattern_analysis

# Получить анализ паттернов для монеты
pattern_result = get_pattern_analysis(
    symbol='BTC',
    signal='LONG',
    current_price=65000.0
)

if pattern_result:
    print(f"Паттернов найдено: {pattern_result['patterns_found']}")
    print(f"Подтверждение: {pattern_result['confirmation']}")
    print(f"Сигнал: {pattern_result['signal']}")
    print(f"Уверенность: {pattern_result['confidence']:.1f}%")
```

### Результат анализа

```python
{
    'confirmation': True,  # Подтверждает ли паттерн сигнал
    'confidence': 78.5,  # Общая уверенность
    'patterns_found': 3,  # Количество найденных паттернов
    'signal': 'BULLISH',  # Общий сигнал паттернов
    'strongest_pattern': {
        'name': 'Ascending Triangle',
        'type': 'continuation',
        'signal': 'BULLISH',
        'confidence': 85.2,
        'description': 'Восходящий треугольник (поддержка растет)'
    },
    'all_patterns': [...]  # Все найденные паттерны
}
```

## Обучение модели

### Сбор данных

Использует те же исторические данные, что и Anomaly Detector:

```bash
# Данные уже собраны
python scripts/ai/collect_historical_data.py --all
```

### Запуск обучения

```bash
# Обучение на всех монетах
python scripts/ai/train_pattern_detector.py --coins 0

# Обучение на 50 монетах
python scripts/ai/train_pattern_detector.py --coins 50

# Кастомное окно анализа
python scripts/ai/train_pattern_detector.py --coins 0 --window 50
```

### Автоматическое обучение

Pattern Detector автоматически переобучается вместе с другими моделями:

- **Частота**: Раз в неделю (по умолчанию)
- **Время**: 3:00 AM (настраивается)
- **Триггер**: AutoTrainer

## Логирование

### Примеры логов

```
[PATTERN] BTC: ✅ ПОДТВЕРЖДЕНИЕ: Паттерны подтверждают LONG (найдено: 3, уверенность: 78.5%)
[PATTERN] BTC:    └─ Ascending Triangle: Восходящий треугольник (поддержка растет)

[PATTERN] ETH: ⚠️ ПРОТИВОРЕЧИЕ: Сигнал LONG, но паттерны указывают на BEARISH (уверенность: 65.2%)
```

## Математические алгоритмы

### Поиск локальных экстремумов

Используется `scipy.signal.argrelextrema`:
- **Order**: 5 свечей
- **Поиск**: Максимумы и минимумы

### Линейная регрессия

Для треугольников и трендовых линий:
- **Метод**: `scipy.stats.linregress`
- **Метрика**: R² (коэффициент детерминации)
- **Порог**: R² > 0.6 (60% объясненной дисперсии)

### Вычисление уверенности

Разные паттерны используют разные метрики:

**Head & Shoulders**:
```python
confidence = (head - max(shoulders)) / head * 100
```

**Double Top/Bottom**:
```python
confidence = (1 - diff_between_peaks / avg_peak) * 100
```

**Triangle**:
```python
confidence = R² * 100
```

**Breakout**:
```python
confidence = (breakout_distance / level) * 20 * 100  # Cap at 100
```

## Производительность

### Скорость
- **Анализ одной монеты**: ~50-100ms
- **Параллельный анализ**: поддерживается

### Требования
- **Минимум свечей**: 100
- **Рекомендуется**: 200+ свечей для точности

## Файлы

### Основные файлы
```
bot_engine/ai/
├── pattern_detector.py        # Основной класс
├── ai_manager.py              # Интеграция с AI системой

scripts/ai/
├── train_pattern_detector.py  # Скрипт обучения

data/ai/models/
├── pattern_detector.pkl       # Обученная модель
└── pattern_scaler.pkl         # Scaler для признаков
```

## Примеры использования

### Базовый пример

```python
from bot_engine.ai.pattern_detector import PatternDetector

detector = PatternDetector()

# Анализ монеты
result = detector.detect_patterns(candles, current_price=65000)

print(f"Найдено паттернов: {len(result['patterns'])}")
print(f"Общий сигнал: {result['signal']}")
print(f"Уверенность: {result['confidence']:.1f}%")

# Вывод всех паттернов
for pattern in result['patterns']:
    print(f"  - {pattern['name']}: {pattern['description']}")
```

### Интеграция с торговым сигналом

```python
# Получить подтверждение паттернов для LONG сигнала
pattern_signal = detector.get_pattern_signal(
    candles=candles,
    current_price=65000,
    signal_type='LONG'
)

if pattern_signal['confirmation']:
    print("✅ Паттерны подтверждают LONG")
else:
    print("⚠️ Паттерны не подтверждают LONG")
```

## Преимущества

✅ **Классические паттерны** - проверенные временем
✅ **Быстрая работа** - алгоритмический подход
✅ **Интерпретируемость** - понятные сигналы
✅ **Без переобучения** - не зависит от конкретной монеты
✅ **Дополнительное подтверждение** - усиление системных сигналов

## Ограничения

⚠️ **Требует истории** - минимум 100 свечей
⚠️ **Волатильность** - на высоковолатильных монетах может быть шум
⚠️ **Ложные пробои** - возможны ложные сигналы на пробоях
⚠️ **Субъективность** - некоторые паттерны могут интерпретироваться по-разному

## Будущие улучшения

- [ ] Добавить больше свечных паттернов (Morning Star, Evening Star, и др.)
- [ ] Волны Эллиотта
- [ ] Fibonacci retracements
- [ ] Support/Resistance zones (не просто уровни)
- [ ] Volume profile analysis

