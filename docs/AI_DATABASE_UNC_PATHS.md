# Работа AI системы с UNC путями

## Поддержка сетевых путей

AI система полностью поддерживает работу с UNC путями (сетевыми путями Windows).

### Пример использования

```bash
# Запуск с локальной директории
cd E:\Drive\TRADEBOT\InfoBot
python ai.py --mode train

# Запуск с сетевого пути
cd \\Evromini\projects\InfoBot
python ai.py --mode train
```

### Как это работает

1. **Автоматическое определение рабочей директории**
   - Система использует `os.getcwd()` для определения текущей рабочей директории
   - Все относительные пути (например, `data/ai/ai_data.db`) автоматически преобразуются в абсолютные пути относительно текущей директории

2. **Поддержка UNC путей**
   - SQLite и Python полностью поддерживают UNC пути
   - Пути вида `\\Server\Share\Path` обрабатываются корректно
   - Файлы БД создаются и читаются с сетевых дисков без проблем

3. **Структура данных**
   - БД создается в `data/ai/ai_data.db` относительно рабочей директории
   - Все остальные файлы (модели, кэши) также используют относительные пути

### Важные замечания

⚠️ **Производительность**
- Работа с БД по сети может быть медленнее, чем локально
- Рекомендуется использовать локальный диск для активной работы
- Сетевой путь подходит для запуска обучения на сервере

⚠️ **Доступ к сети**
- Убедитесь, что у вас есть права доступа к сетевой папке.
- Откройте **общий доступ по сети** к папке проекта (и к `data`) — иначе возможны «disk I/O error» и «файл занят» (WinError 32/33). См. **Устранение проблем** ниже.
- Проверьте, что путь доступен: `dir \\Evromini\projects\InfoBot`

✅ **Параллельная работа на разных ПК**
- Система поддерживает параллельный запуск `ai.py` на нескольких ПК одновременно
- Автоматическая координация через таблицу `training_locks` в БД
- Каждый процесс обрабатывает только доступные символы (не занятые другими процессами)
- Блокировки автоматически истекают через 120 минут (на случай зависания процесса)
- WAL режим SQLite обеспечивает параллельные чтения и записи без конфликтов

⚠️ **Блокировки файлов**
- SQLite использует файловые блокировки для обеспечения целостности
- При работе по сети возможны задержки из-за сетевых блокировок
- **НО:** Система координации через БД позволяет безопасно работать параллельно

### Тестирование

Для проверки работы с UNC путями:

```bash
# 1. Проверка доступности пути
python -c "import os; print(os.path.exists(r'\\Evromini\projects\InfoBot'))"

# 2. Запуск тестов БД
python scripts/test_ai_database.py

# 3. Проверка инициализации БД
python -c "from bot_engine.ai.ai_database import get_ai_database; db = get_ai_database(); print(db.db_path)"
```

### Примеры использования

#### Локальный запуск
```bash
cd E:\Drive\TRADEBOT\InfoBot
python ai.py --mode train
```

#### Запуск с сервера
```bash
cd \\Evromini\projects\InfoBot
python ai.py --mode train
```

#### Смешанный режим (локальный запуск, данные с сервера)
```bash
# На локальной машине
cd E:\Drive\TRADEBOT\InfoBot

# Монтируем сетевой диск или используем прямой путь
# В коде можно указать явный путь к БД:
python -c "from bot_engine.ai.ai_database import AIDatabase; db = AIDatabase(r'\\Evromini\projects\InfoBot\data\ai\ai_data.db')"
```

### Структура файлов

При запуске с любой директории структура остается одинаковой:

```
[рабочая_директория]/
├── ai.py                    # Главный скрипт
├── bot_engine/
│   └── ai/
│       └── ai_database.py   # Модуль БД
├── data/
│   └── ai/
│       └── ai_data.db       # База данных (создается автоматически)
└── ...
```

Все пути относительно рабочей директории, поэтому система работает одинаково локально и по сети.

---

## Параллельная работа на разных ПК

### Обзор

AI система поддерживает параллельный запуск `ai.py` на нескольких компьютерах одновременно для ускорения обучения. Система автоматически координирует работу через базу данных, предотвращая конфликты и дублирование работы.

### Как это работает

1. **Координация через БД**
   - Таблица `training_locks` в БД отслеживает, какой процесс обрабатывает какой символ
   - Каждый процесс получает уникальный ID: `hostname_PID_timestamp`
   - Перед обработкой символа процесс пытается заблокировать его в БД
   - Если символ уже заблокирован другим процессом - он пропускается

2. **Автоматическое распределение**
   - При запуске каждый процесс получает список доступных символов
   - Символы, обрабатываемые другими процессами, автоматически исключаются
   - Процессы не конфликтуют и не обрабатывают одни и те же символы

3. **Безопасность и отказоустойчивость**
   - Блокировки автоматически истекают через 120 минут (настраивается)
   - Если процесс завис или упал - блокировка автоматически освобождается
   - WAL режим SQLite обеспечивает параллельные чтения и записи

### Настройка

#### Требования

1. **Общая БД на сетевом диске**
   ```
   \\Evromini\projects\InfoBot\data\ai\ai_data.db
   ```

2. **Доступ к сетевой папке**
   - Все ПК должны иметь доступ к сетевой папке
   - Проверьте доступ: `dir \\Evromini\projects\InfoBot`

3. **Одинаковый путь к БД**
   - Все ПК должны запускать `ai.py` из одной директории (сетевой)
   - Или использовать явный путь к БД (см. ниже)

#### Запуск на разных ПК

**Вариант 1: Запуск из сетевой папки (рекомендуется)**

```bash
# На ПК 1
cd \\Evromini\projects\InfoBot
python ai.py --mode train

# На ПК 2 (параллельно)
cd \\Evromini\projects\InfoBot
python ai.py --mode train

# На ПК 3 (параллельно)
cd \\Evromini\projects\InfoBot
python ai.py --mode train
```

**Вариант 2: Явный путь к БД**

```python
# В коде можно указать явный путь к БД:
from bot_engine.ai.ai_database import AIDatabase

db = AIDatabase(r'\\Evromini\projects\InfoBot\data\ai\ai_data.db')
```

### Пример работы

При запуске нескольких процессов вы увидите в логах:

```
[AI] 🔄 Параллельная обработка: доступно 185/556 монет (остальные обрабатываются другими процессами)
[AI]   ⏭️ BTCUSDT: пропущен (обрабатывается другим процессом)
[AI]   ⏭️ ETHUSDT: пропущен (обрабатывается другим процессом)
[AI]   🎓 [1/185] Начало обработки ADAUSDT...
```

### Технические детали

#### Таблица training_locks

```sql
CREATE TABLE training_locks (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    symbol TEXT NOT NULL,
    process_id TEXT NOT NULL,
    hostname TEXT,
    locked_at TEXT NOT NULL,
    expires_at TEXT NOT NULL,
    status TEXT NOT NULL DEFAULT 'PROCESSING',
    UNIQUE(symbol)
)
```

#### Методы координации

- `try_lock_symbol(symbol, process_id, hostname, lock_duration_minutes=120)` - блокирует символ
- `release_lock(symbol, process_id)` - освобождает блокировку
- `get_available_symbols(all_symbols, process_id, hostname)` - получает доступные символы
- `extend_lock(symbol, process_id, additional_minutes=30)` - продлевает блокировку

#### Автоматическая очистка

Истекшие блокировки автоматически удаляются при:
- Попытке заблокировать новый символ
- Получении списка доступных символов
- Продлении блокировки

### Рекомендации

✅ **Рекомендуется:**
- Использовать сетевой диск для БД
- Запускать процессы на разных ПК для максимальной производительности
- Мониторить логи для отслеживания прогресса

⚠️ **Ограничения:**
- Блокировки истекают через 120 минут (настраивается в коде)
- При очень медленной сети возможны задержки
- Рекомендуется не более 5-10 параллельных процессов на одну БД

### Мониторинг

Проверить активные блокировки:

```python
from bot_engine.ai.ai_database import get_ai_database

db = get_ai_database()
with db._get_connection() as conn:
    cursor = conn.cursor()
    cursor.execute("SELECT symbol, process_id, hostname, locked_at, expires_at FROM training_locks")
    for row in cursor.fetchall():
        print(f"{row['symbol']}: {row['hostname']} ({row['process_id']})")
```

### Устранение проблем

**Проблема:** При запуске с UNC — «disk I/O error», «файл занят» (WinError 32/33), не удаётся создать резервную копию БД

**Решение:** Выдайте **права** на папку `data` (и при необходимости на корень проекта) и откройте **общий доступ по сети** к этой папке. После этого ошибки часто пропадают.

- Права: папка должна быть доступна на запись для пользователя, под которым запускается InfoBot.
- Общий доступ по сети: в свойствах папки (ПКМ → Общий доступ) включите общий доступ и при необходимости настройте разрешения для нужных пользователей.
- Проверка: `dir \\SERVER\share\InfoBot\data` — папка должна открываться без ошибок.

**Проблема:** Символы не обрабатываются, все заблокированы

**Решение:** Проверьте истекшие блокировки:
```python
# Очистить все истекшие блокировки вручную
from bot_engine.ai.ai_database import get_ai_database
from datetime import datetime

db = get_ai_database()
with db._get_connection() as conn:
    cursor = conn.cursor()
    cursor.execute("DELETE FROM training_locks WHERE expires_at < ?", (datetime.now().isoformat(),))
    conn.commit()
```

**Проблема:** Медленная работа при параллельном доступе

**Решение:** 
- Убедитесь, что используется WAL режим (включен по умолчанию)
- Проверьте скорость сети
- Уменьшите количество параллельных процессов

