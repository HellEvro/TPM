# 🔄 Процесс торговли: от загрузки свечей до открытия сделки

## 📋 Общая схема процесса

```
1. Загрузка свечей → 2. Расчет RSI → 3. Расчет EMA → 4. Расчет тренда → 
5. Расчет сигналов → 6. Проверка зрелости → 7. Фильтры → 8. Решение бота
```

---

## 🚀 Детальное описание этапов

### ЭТАП 1: Загрузка свечей (`load_all_coins_candles_fast()`)

**Место:** `bots_modules/candle_loader.py` → `bots_modules/filters.py::get_coin_candles_only()`

**Что происходит:**
1. Загружаются последние N свечей с биржи (N = `max_candles_limit` из конфига, по умолчанию 2000)
2. **СРАЗУ** сохраняются в файл `data/candles_cache/{timeframe}/{symbol}.json`
3. **В ТОМ ЖЕ ЦИКЛЕ:**
   - Рассчитывается RSI по последним 14+ свечам
   - **RSI сохраняется в файл** (`rsi_last_candle`)
   - Рассчитывается зрелость монеты (если ≥400 свечей)
   - Зрелость сохраняется в хранилище

**Результат:**
- Файл свечей содержит: `candles`, `rsi_last_candle`, `count`, `last_update`
- Каждая монета обрабатывается **по отдельности** (не скопом)

**Оптимизация:**
- ✅ Файлы сохраняются сразу, не ждут конца пакета
- ✅ RSI и зрелость считаются в одном цикле
- ✅ Время загрузки: ~10-15 сек для 500+ монет

---

### ЭТАП 2: Расчет RSI (`calculate_only_rsi()`)

**Место:** `bots_modules/filters.py::calculate_only_rsi()`  
**Вызывается из:** `bots_modules/filters.py::load_all_coins_rsi()` → ЭТАП 1/4

**Что происходит:**
1. **ПОПЫТКА 1:** Читает RSI из файла свечей (если есть)
   - Если RSI найден → используется **без пересчета**
   - Если RSI есть → возврат результата, переход к следующему этапу
2. **ПОПЫТКА 2:** Если RSI нет в файле:
   - Читает свечи из БД
   - Рассчитывает RSI по последним 14+ свечам
   - **ЗАПИСЫВАЕТ RSI в файл** для следующего раза
   - Возвращает результат

**Результат:**
```python
{
    'symbol': 'BTC',
    'rsi_1d': 45.23,  # или None если недостаточно свечей
    'rsi_available': True/False,
    'candles': [...],
    'closes': [...],
    'candles_count': 2000
}
```

**Оптимизация:**
- ✅ RSI из файла = 0 вычислений (мгновенно)
- ✅ RSI расчет = только если нужно
- ✅ RSI автоматически сохраняется для кэширования

---

### ЭТАП 3: Расчет оптимальных EMA (`calculate_ema_for_coin()`)

**Место:** `bots_modules/filters.py::calculate_ema_for_coin()`  
**Вызывается из:** `bots_modules/filters.py::load_all_coins_rsi()` → ЭТАП 2/4

**Что происходит:**
1. Читает оптимальные периоды EMA из файла `data/optimal_ema_data.json`
2. Если периодов нет → использует дефолтные (50/200)
3. Сохраняет периоды в `coin_data['ema_periods']`

**Результат:**
```python
{
    'ema_periods': {
        'ema_short': 50,
        'ema_long': 200,
        'accuracy': 0.75,
        'analysis_method': 'backtest'
    }
}
```

---

### ЭТАП 4: Расчет тренда (`calculate_trend_for_coin()`)

**Место:** `bots_modules/filters.py::calculate_trend_for_coin()`  
**Вызывается из:** `bots_modules/filters.py::load_all_coins_rsi()` → ЭТАП 3/4

**Что происходит:**
1. Использует свечи из `coin_data['candles']` (уже загружены)
2. Рассчитывает EMA short и EMA long по оптимальным периодам
3. Анализирует:
   - Пересечение EMA (short выше/ниже long)
   - Наклон EMA long (вверх/вниз/горизонтально)
   - Подтверждение ценой (close выше/ниже EMA)
4. Определяет тренд: `UP` / `DOWN` / `NEUTRAL`

**Результат:**
```python
{
    'trend_1d': 'UP',  # или 'DOWN' или 'NEUTRAL'
    'ema_short': 2.5,
    'ema_long': 2.3,
    'ema_long_slope': 0.05
}
```

**Оптимизация:**
- ✅ Нет запросов к бирже (использует свечи из БД)
- ✅ Быстро (~1 мс на монету)

---

### ЭТАП 5: Расчет сигналов (`calculate_signals_for_coin()`)

**Место:** `bots_modules/filters.py::calculate_signals_for_coin()`  
**Вызывается из:** `bots_modules/filters.py::load_all_coins_rsi()` → ЭТАП 4/4

**Что происходит:**
1. Определяет зону RSI:
   - `OVERSOLD` если RSI ≤ 29 (из конфига)
   - `OVERBOUGHT` если RSI ≥ 71 (из конфига)
   - `NEUTRAL` иначе
2. Генерирует сигнал:
   - `ENTER_LONG` если RSI ≤ 29 и тренд не DOWN
   - `ENTER_SHORT` если RSI ≥ 71 и тренд не UP
   - `WAIT` иначе
3. Рассчитывает изменения за 24ч

**Результат:**
```python
{
    'signal': 'ENTER_LONG',  # или 'ENTER_SHORT' или 'WAIT'
    'rsi_zone': 'OVERSOLD',  # или 'OVERBOUGHT' или 'NEUTRAL'
    'change24h': 5.2  # процент изменения за 24ч
}
```

---

### ЭТАП 6: Проверка зрелости монеты (уже выполнена на ЭТАПЕ 1)

**Место:** `bots_modules/maturity.py::check_coin_maturity_with_storage()`  
**Выполняется:** При загрузке свечей (ЭТАП 1)

**Что проверяется:**
1. Количество свечей (минимум 400 по умолчанию)
2. RSI история (был ли RSI ≤ 35 и ≥ 65 за последние N свечей)
3. Волатильность (минимум 5% по умолчанию)

**Результат:**
- Зрелые монеты сохраняются в `data/mature_coins_{timeframe}.json`
- При проверке читается из хранилища (быстро)

---

### ЭТАП 7: Применение торговых фильтров (`_process_filters()`)

**Место:** `bots_modules/continuous_data_loader.py::_process_filters()`  
**Вызывается:** После расчета RSI/EMA/Тренд/Сигналов

**Фильтры по порядку:**
1. **Зрелость:** Только зрелые монеты могут торговаться
2. **Область действия (scope):** whitelist/blacklist/all
3. **RSI Time Filter:** Проверка на экстремумы и спокойствие рынка
4. **Exit Scam Filter:** Проверка на pump & dump через turnover
5. **Анти-долистинг:** Исключение монет с делистингом
6. **Тренд фильтры:**
   - `avoid_down_trend`: Блокировка LONG при DOWN тренде
   - `avoid_up_trend`: Блокировка SHORT при UP тренде
7. **AI фильтры** (если включены)
8. **Существующие позиции:** Не входить если уже есть позиция

**Результат:**
```python
filtered_coins = {
    'BTC': {
        'signal': 'ENTER_LONG',
        'rsi': 25.5,
        'trend': 'NEUTRAL',
        'is_mature': True,
        'passed_filters': True
    },
    ...
}
```

---

### ЭТАП 8: Решение бота о входе (`TradingBot::_handle_idle_state()`)

**Место:** `bot_engine/trading_bot.py::_handle_idle_state()`  
**Вызывается:** Непрерывно для каждого бота в состоянии `IDLE`

**Что проверяется:**
1. **Защита от повторного входа:**
   - Если был стоп-лосс < 30 минут назад → блокировка
   - `last_stop_time` проверяется перед входом
2. **Сигнал от системы:**
   - Читает `filtered_coins[symbol]`
   - Если `signal == 'ENTER_LONG'` → открывает LONG
   - Если `signal == 'ENTER_SHORT'` → открывает SHORT
   - Если `signal == 'WAIT'` → остается в IDLE
3. **Подтверждение входа:**
   - Проверка лимитов (max_concurrent, risk_cap)
   - Проверка баланса
   - Создание позиции на бирже

**Результат:**
- Бот переходит в состояние `LONG` или `SHORT`
- Позиция открывается на бирже
- Устанавливается стоп-лосс и тейк-профит

---

## ⏱️ Временная шкала процесса

```
0:00 - continuous_data_loader запускается
0:01 - Загрузка свечей (10-15 сек, 500+ монет)
      ├─ Каждая монета: свечи + RSI + зрелость → файл
      └─ Все файлы сохранены по отдельности
0:15 - Расчет RSI (если не было в файле)
      ├─ Чтение RSI из файла (быстро)
      └─ Расчет только для новых/измененных
0:20 - Расчет EMA (чтение из файла, быстро)
0:22 - Расчет трендов (использует свечи из БД, быстро)
0:25 - Расчет сигналов (быстро)
0:27 - Применение фильтров (быстро)
0:30 - Передача отфильтрованных монет автоботу
0:31 - Боты проверяют свои сигналы каждую секунду
      └─ Если сигнал = ENTER_LONG/SHORT → вход в сделку
```

**Общее время одного цикла:** ~30-60 секунд

---

## 🔄 Непрерывный цикл

Процесс повторяется каждые N минут (из конфига `update_interval`):
1. Загрузка свечей → обновление файлов
2. Пересчет RSI только если свечи изменились
3. Обновление трендов и сигналов
4. Обновление фильтров
5. Боты проверяют новые сигналы

**Оптимизация повторных загрузок:**
- Свечи обновляются только если изменились (append mode)
- RSI из файла (быстро, без пересчета)
- Только новые/измененные данные пересчитываются

---

## 📊 Пример полного процесса для одной монеты

**BTC на таймфрейме 1d:**

1. **Загрузка:**
   ```
   Загружено 2000 свечей → сохранено в data/candles_cache/1d/BTC.json
   RSI рассчитан = 45.23 → сохранен в файл как rsi_last_candle
   Зрелость = True → сохранена в mature_coins_1d.json
   ```

2. **Расчет RSI (этап 2):**
   ```
   Читаем файл → rsi_last_candle = 45.23 → используем БЕЗ пересчета!
   ```

3. **Расчет EMA:**
   ```
   Читаем optimal_ema_data.json → ema_short=50, ema_long=200
   ```

4. **Расчет тренда:**
   ```
   EMA(50) = 2.5, EMA(200) = 2.3
   EMA(50) > EMA(200) и EMA(200) растет → тренд = UP
   ```

5. **Расчет сигнала:**
   ```
   RSI = 45.23 (NEUTRAL)
   Тренд = UP
   Сигнал = WAIT (нет экстремумов RSI)
   ```

6. **Фильтры:**
   ```
   Зрелость: ✅ (есть в mature_coins)
   RSI Time: ✅ (нет экстремумов)
   Exit Scam: ✅ (turnover нормальный)
   Тренд: ✅ (не блокирует)
   Итог: Проходит все фильтры, но сигнал = WAIT
   ```

7. **Решение бота:**
   ```
   Бот читает signal = WAIT → остается в IDLE
   Ждет следующего цикла или изменения сигнала
   ```

---

## 🎯 Ключевые оптимизации

1. **Файлы сохраняются сразу** (не скопом) → нет блокировок
2. **RSI кэшируется в файле** → мгновенное чтение
3. **Зрелость рассчитывается при загрузке** → один раз
4. **Тренд использует свечи из БД** → нет запросов к бирже
5. **Фильтры применяются параллельно** → быстро

**Результат:** Полный цикл от загрузки до решения бота занимает ~30-60 секунд для 500+ монет.

