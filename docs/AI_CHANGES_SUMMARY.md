# 📋 Краткая справка изменений AI системы

## 🎯 Основные изменения

### 1. 📊 Чтение свечей из `candles_cache.json`
- **Было:** AI получал свечи через API запросы
- **Стало:** AI читает свечи напрямую из `data/candles_cache.json` (которые `bots.py` собирает постоянно)
- **Результат:** Нет дополнительных запросов к бирже, используется уже собранная информация
- **Файлы:** `bot_engine/ai/ai_data_collector.py`, `bot_engine/ai/ai_trainer.py`, `bot_engine/ai/ai_backtester_new.py`

### 2. 📈 Загрузка полной истории свечей с пагинацией
- **Создан модуль:** `bot_engine/ai/ai_candles_loader.py`
- **Функционал:** 
  - Загружает ВСЕ доступные свечи для всех монет (до 2000 свечей за запрос)
  - Использует пагинацию для получения максимальной истории (~40,000 свечей на монету)
  - Сохраняет в `data/ai/candles_full_history.json`
- **Результат:** AI получает максимально возможную историю для обучения (~27 лет данных для 6H свечей)

### 3. 🔧 Исправления
- **Синтаксическая ошибка:** Исправлен неправильный `except` блок в `ai_data_collector.py`
- **Кодировка:** Добавлена поддержка UTF-8 для Windows консоли в скриптах

### 4. 📁 Организация документации
- **Изменение:** Все `.md` файлы перемещены в `docs/` (кроме `README.md`)
- **Результат:** Чистая структура проекта, вся документация в одном месте

### 5. 💾 Автоматическое разбиение больших файлов
- **Проблема:** Git не поддерживает файлы >100MB
- **Решение:** 
  - Автоматическое разбиение `market_data.json` на части по 100MB при сохранении
  - Автоматическая сборка частей обратно при загрузке
  - Части можно коммитить в Git (каждая <100MB)
- **Файлы:** 
  - `bot_engine/ai/ai_data_collector.py` (методы `_save_data`, `_load_data`)
  - `scripts/split_large_json.py` (утилита для ручного разбиения)
  - `.gitignore` (игнорирует основной файл, разрешает части)

### 6. 🗑️ Очистка Git истории
- **Действие:** Удален `market_data.json` (391MB) из истории Git через `git filter-branch`
- **Результат:** Репозиторий готов к push без ошибок размера файлов

## 📊 Статистика данных

### До изменений:
- Свечей на монету: ~1000 (из `candles_cache.json`)
- Всего свечей: ~554,000
- Размер `market_data.json`: 405MB (нельзя коммитить)

### После изменений:
- Свечей на монету: до ~40,000 (с пагинацией)
- Всего свечей: до ~22,000,000
- Размер `market_data.json`: разбивается на части <100MB каждая

## 🔄 Процесс работы

### Сбор данных:
```
bots.py → собирает свечи → candles_cache.json
  ↓
AI → читает candles_cache.json напрямую
  ↓
AI → загружает полную историю с пагинацией → candles_full_history.json
  ↓
AI → сохраняет данные → market_data.json (автоматически разбивается если >100MB)
```

### Загрузка данных:
```
AI → проверяет наличие частей (*.part1, *.part2, ...)
  ↓
Если части есть → собирает их в один объект
Если частей нет → загружает обычный файл
```

## 📝 Важные файлы

- `bot_engine/ai/ai_data_collector.py` - сбор данных с поддержкой разбиения
- `bot_engine/ai/ai_candles_loader.py` - загрузка полной истории свечей
- `scripts/split_large_json.py` - утилита для ручного разбиения файлов
- `.gitignore` - игнорирует большие файлы, разрешает части

## ✅ Результат

- ✅ AI использует уже собранные данные без дополнительных запросов
- ✅ AI получает максимальную историю для обучения
- ✅ Большие файлы автоматически разбиваются для Git
- ✅ Система готова к работе и коммитам

