# 🔐 InfoBot AI Premium

**Приватный репозиторий для премиум ИИ модулей**

⚠️ **ВАЖНО:** Этот код НЕ должен попадать в публичный репозиторий!

---

## 📦 Структура

```
InfoBot_AI_Premium/
├── modules/          # Реальные реализации ИИ модулей (копии из основного проекта)
├── license/          # Система лицензирования
├── build/            # Скрипты сборки и защиты
└── dist/             # Собранные защищенные модули (.pyd/.so)
```

---

## 🔨 Сборка

### 1. Копирование модулей

```bash
# Копируем актуальные модули из основного проекта
python build/prepare_for_build.py
```

### 2. Компиляция в .pyd/.so

```bash
# Windows
python build/build_premium.py build_ext --inplace

# Linux
python build/build_premium.py build_ext --inplace
```

### 3. Обфускация

```bash
python build/obfuscate.py
```

### 4. Упаковка

```bash
python build/package.py
```

---

## 📋 Зависимости

```bash
pip install Cython pyarmor cryptography
```

---

## 🚀 Установка пользователем

```bash
pip install dist/infobot_ai_premium-1.0.0-py3-none-any.whl
```

---

**Для разработчиков InfoBot только!** 🔒

