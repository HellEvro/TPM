# 📦 LICENSE GENERATOR - ФИНАЛЬНАЯ СТРУКТУРА

## ⚠️ ВАЖНО: Эта папка НЕ попадает в публичную версию!

## 📁 Структура:

```
license_generator/
├── source/                          # ИСХОДНИКИ (только для разработчика!)
│   ├── ai_manager_source.py        # Защищенный ai_manager с проверкой лицензий
│   ├── _premium_loader_source.py    # Старый загрузчик (для справки)
│   └── license_checker.py          # Модуль проверки (если будем компилировать)
├── generate_license.py              # Генератор лицензий для клиентов
├── license_manager.py              # Менеджер лицензий
├── hardware_id.py                  # Получение HWID
├── license_types.py                # Типы лицензий
└── generated_licenses/             # Сгенерированные .lic файлы

ПУБЛИЧНАЯ ВЕРСИЯ получает только:
├── bot_engine/ai/ai_manager.py     # С защищенной проверкой лицензий
└── БЕЗ license_generator/!
```

## 🔐 Безопасность:

1. **Исходники** `source/` хранятся ТОЛЬКО у разработчика
2. **Генератор** `generate_license.py` НЕ копируется в публику
3. **Публика** получает только `ai_manager.py` с встроенной проверкой
4. **ВНИМАНИЕ:** Ключи видны в коде `ai_manager.py` - это временное решение!
5. **РЕКОМЕНДАЦИЯ:** Добавить серверную активацию для production

## 🚀 Использование:

### Генерация лицензии:
```bash
cd license_generator
python generate_license.py <HWID> <DAYS>
```

### Обновление исходников:
```bash
cp bot_engine/ai/ai_manager.py license_generator/source/ai_manager_source.py
```

### Копирование в публику:
```bash
python ../scripts/copy_to_public.py
```

## ✅ Что сделано:

- ✅ Исходники в `license_generator/source/`
- ✅ `ai_manager.py` с защищенной проверкой лицензий
- ✅ Генератор лицензий работает
- ✅ Публичная версия получает только защищенный код
- ✅ `license_generator/` НЕ копируется в публику

## 🔐 Лицензирование и защита (актуальный пайплайн)

- `scripts/activate_premium.py` генерирует HWID, `.lic` создаётся в этой папке; тест `tests/test_hwid_check.py` контролирует, что HWID совпадает во всех точках.
- `bot_engine/ai/license_checker.pyc` (и исходник в `source/@source/ai_launcher_source.py`) валидируют лицензии **до запуска** любых AI подсистем, кэшируют статус и блокируют AI при истечении срока.
- `bot_engine/ai/_infobot_ai_protected.py` загружает скомпилированный `_ai_launcher.pyc`, а корневой `ai.py` экспортирует только публичные функции.
- `license_generator/build_ai_launcher.py` пересобирает `_ai_launcher.pyc`, обновляет обёртки `ai.py` и `_infobot_ai_protected.py`, чтобы в репозиторий/публичную сборку попадали только защищённые файлы.
- Премиум-функции (`bot_engine/ai/smart_risk_manager.py`, `bot_engine/ai/ml_risk_predictor.py`, премиальные SL/TP) проверяют `check_premium_license()` и отключаются, если лицензия недоступна.

## 📝 Примечание:

При изменении `bot_engine/ai/ai_manager.py` обновляй исходники:
```bash
cp bot_engine/ai/ai_manager.py license_generator/source/ai_manager_source.py
```

**Система готова к работе!**

