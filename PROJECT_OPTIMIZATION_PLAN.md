# ПЛАН ОПТИМИЗАЦИИ СТРУКТУРЫ ПРОЕКТА

## 📁 ТЕКУЩАЯ СТРУКТУРА (ПРОБЛЕМЫ)

### ❌ Файлы в корне (нужно переместить):
- `check_*.py` → `scripts/check/`
- `find_*.py` → `scripts/analysis/`
- `test_*.py` → `tests/`
- `*_fix.py` → `scripts/fix/`
- `*.bat` → `scripts/start/`
- `*.md` (документация) → `docs/`

### ❌ Дублирующиеся файлы:
- `utils/rsi_calculator.py` (дубликат в `bot_engine/utils/rsi_utils.py`)
- `Задачи.txt` (неиспользуемый)

### ❌ Неиспользуемые файлы:
- `app.py` (старый файл)
- `bot_history.py` (если не используется)
- `protector.py` (если не используется)

## 🎯 ПЛАН РЕОРГАНИЗАЦИИ

### 1. Создать недостающие папки:
```
scripts/
├── check/          # Скрипты проверки
├── analysis/       # Скрипты анализа
├── fix/           # Скрипты исправления
├── start/         # Скрипты запуска
└── sync/          # Скрипты синхронизации
```

### 2. Переместить файлы:

#### В `scripts/check/`:
- `check_all_mature_coins.py`
- `check_maturity_logic.py`
- `check_signals.py`
- `check_specific_coins.py`
- `check_syntax.py`

#### В `scripts/analysis/`:
- `find_extreme_rsi.py`
- `find_good_candidates.py`

#### В `scripts/fix/`:
- `fix_remaining.py`
- `format_code.py`
- `quick_fix.py`

#### В `scripts/start/`:
- `*.bat` файлы
- `start_all.py`
- `hot_reload_bots.py`
- `restart_bots.py`

#### В `scripts/sync/`:
- `force_rsi_update.py`
- `optimal_ema.py`
- `sync_optimal_ema.py`

#### В `tests/`:
- `test_*.py` файлы
- `run_tests.bat`
- `test_output.txt`

#### В `docs/`:
- Все `*.md` файлы из корня

### 3. Удалить неиспользуемые файлы:
- `Задачи.txt`
- `app.py` (если не используется)
- `bot_history.py` (если не используется)
- `protector.py` (если не используется)

### 4. Обновить импорты:
- Проверить все импорты после перемещения файлов
- Обновить пути в скриптах

## ✅ ИТОГОВАЯ СТРУКТУРА

```
InfoBot/
├── app/                    # Основное приложение
├── bot_engine/            # Движок ботов
├── exchanges/             # Подключения к биржам
├── static/               # Статические файлы (CSS, JS)
├── templates/            # HTML шаблоны
├── scripts/              # Утилиты и скрипты
│   ├── check/           # Проверки
│   ├── analysis/        # Анализ
│   ├── fix/            # Исправления
│   ├── start/          # Запуск
│   └── sync/           # Синхронизация
├── tests/               # Тесты
├── docs/               # Документация
├── utils/              # Общие утилиты
├── data/               # Данные
├── logs/               # Логи
├── bots.py             # Главный файл
├── color_logger.py     # Логгер
└── requirements.txt    # Зависимости
```

## 🔧 ДЕЙСТВИЯ

1. Создать папки
2. Переместить файлы
3. Удалить неиспользуемые
4. Обновить импорты
5. Проверить работоспособность
