# 🎯 Применение обученных стратегий AI через bots.py

## Архитектура применения

```
┌─────────────────┐
│   ai.py         │  ← Обучение на истории трейдов
│   (Обучение)    │     и исторических данных
└────────┬────────┘
         │
         │ Сохраняет модели в data/ai/models/
         │
         ▼
┌─────────────────┐
│  bots.py        │  ← Применяет обученные стратегии
│  (Торговля)     │     при принятии решений
└─────────────────┘
```

## Как это работает

### 1. Обучение (ai.py)

AI система обучается на:
- Истории трейдов из `bot_history.py`
- Параметрах стратегии
- Исторических данных (свечи, RSI, индикаторы)

Результат: обученные модели сохраняются в `data/ai/models/`

### 2. Применение (bots.py)

При принятии решения об открытии позиции:

1. **Проверка базовых условий** (RSI, тренд, фильтры)
2. **🤖 Проверка AI предсказания** (если включено)
   - AI анализирует текущую ситуацию
   - Предсказывает вероятность прибыли
   - Дает рекомендацию (LONG/SHORT/WAIT)
3. **Принятие решения** на основе обоих факторов

## Включение AI в bots.py

### Вариант 1: Через конфигурацию Auto Bot

Добавьте в конфигурацию Auto Bot:

```json
{
  "ai_enabled": true,
  "ai_min_confidence": 0.7
}
```

### Вариант 2: Программно

В `bots_modules/bot_class.py` уже добавлена интеграция:

```python
# В методе should_open_long() и should_open_short()
# автоматически проверяется AI предсказание, если ai_enabled = True
```

## Логика применения

### Когда AI блокирует сделку:

```
Базовые условия ✅ (RSI OK, тренд OK)
    ↓
AI предсказание ❌ (низкая уверенность или сигнал WAIT)
    ↓
Сделка НЕ открывается
```

### Когда AI подтверждает сделку:

```
Базовые условия ✅ (RSI OK, тренд OK)
    ↓
AI предсказание ✅ (высокая уверенность, сигнал LONG/SHORT)
    ↓
Сделка открывается
```

## Пример работы

### Сценарий 1: AI блокирует сделку

```
[INFO] [NEW_BOT_BTCUSDT] ✅ RSI 28.5 <= 29 - условие выполнено
[INFO] [NEW_BOT_BTCUSDT] ✅ Тренд UP - условие выполнено
[INFO] [NEW_BOT_BTCUSDT] 🤖 AI блокирует LONG: AI prediction (confidence: 45%)
[INFO] [NEW_BOT_BTCUSDT] ⏳ Сделка не открыта (AI блокировка)
```

### Сценарий 2: AI подтверждает сделку

```
[INFO] [NEW_BOT_BTCUSDT] ✅ RSI 28.5 <= 29 - условие выполнено
[INFO] [NEW_BOT_BTCUSDT] ✅ Тренд UP - условие выполнено
[INFO] [NEW_BOT_BTCUSDT] 🤖 AI подтверждает LONG (уверенность: 82%)
[INFO] [NEW_BOT_BTCUSDT] ✅ Открываем LONG (RSI: 28.5)
```

## Настройки AI

### В конфигурации Auto Bot:

```python
{
    "ai_enabled": True,              # Включить использование AI
    "ai_min_confidence": 0.7,        # Минимальная уверенность (0.0-1.0)
    "ai_override_original": False    # Переопределять оригинальный сигнал
}
```

### Параметры:

- **ai_enabled**: Включить/выключить использование AI предсказаний
- **ai_min_confidence**: Минимальная уверенность AI для применения сигнала (по умолчанию 0.7 = 70%)
- **ai_override_original**: Если True, AI сигнал полностью заменяет оригинальный (по умолчанию False - AI только блокирует или подтверждает)

## Оптимизация параметров

AI может также оптимизировать параметры стратегии:

```python
from ai_modules.ai_integration import get_optimized_bot_config

# Получить оптимизированные параметры для символа
optimized_config = get_optimized_bot_config('BTCUSDT')

# Применить оптимизированные параметры
if optimized_config:
    # Обновить конфигурацию бота
    update_bot_config('BTCUSDT', optimized_config)
```

## Мониторинг

### Логи AI применения:

```bash
tail -f logs/bots.log | grep "AI"
```

### Статистика:

- Количество сделок, заблокированных AI
- Количество сделок, подтвержденных AI
- Средняя уверенность AI предсказаний

## Важные замечания

1. **AI не заменяет базовую логику** - он только дополняет её
2. **Требуется обучение** - минимум 50 закрытых сделок для качественных предсказаний
3. **Модели обновляются** - автоматически каждые 1 час (настраивается)
4. **Безопасность** - AI может только блокировать или подтверждать сделки, не открывает их самостоятельно

## Отключение AI

Если нужно временно отключить AI:

```python
# В конфигурации Auto Bot
{
    "ai_enabled": False
}
```

Или просто не запускать `ai.py` - система будет работать без AI предсказаний.

