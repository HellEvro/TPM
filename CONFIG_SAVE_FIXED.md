# Исправление проблемы сохранения конфигурации

## Проблема
Кнопки сохранения конфигурации работали, но после перезагрузки страницы значения возвращались к старым. В логах показывалось что конфигурация сохраняется, но изменения не применялись.

## Причина
**Конфликт между двумя файлами конфигурации:**

1. `data/auto_bot_config.json` - основной файл конфигурации (сохраняется через API)
2. `data/bots_state.json` - файл состояния ботов (содержал копию конфигурации)

### Последовательность загрузки (НЕПРАВИЛЬНАЯ):
1. ✅ `load_auto_bot_config()` - загружает из `auto_bot_config.json`
2. ❌ `load_bots_state()` - **ПЕРЕЗАПИСЫВАЕТ** из `bots_state.json`

### Проблемный код:
```python
# В load_bots_state() - СТРОКА 508:
bots_data['auto_bot_config'].update(state_data['auto_bot_config'])
```

## Решение

### ✅ Исправлена функция `load_bots_state()`
**Файл**: `bots_modules/sync_and_cache.py`

**Удален код перезаписи конфигурации:**
```python
# УДАЛЕНО:
bots_data['auto_bot_config'].update(state_data['auto_bot_config'])

# ЗАМЕНЕНО НА:
logger.info(f"[LOAD_STATE] ⚙️ Конфигурация Auto Bot НЕ загружается из bots_state.json")
logger.info(f"[LOAD_STATE] 💡 Конфигурация загружается только из auto_bot_config.json")
```

### ✅ Теперь правильная последовательность:
1. ✅ `load_auto_bot_config()` - загружает из `auto_bot_config.json`
2. ✅ `load_bots_state()` - загружает только состояние ботов, НЕ перезаписывает конфигурацию

## Результат

### ✅ Конфигурация сохраняется правильно:
- Изменения сохраняются в `data/auto_bot_config.json`
- При перезагрузке страницы значения НЕ возвращаются к старым
- Конфигурация загружается только из одного источника

### ✅ Логи показывают корректные изменения:
```
[CONFIG] ✓ max_concurrent_bots: 5 → 51
[SAVE_CONFIG] ✅ Конфигурация автобота сохранена в data/auto_bot_config.json
```

### ✅ После перезагрузки:
- Значения остаются новыми (51)
- Конфигурация не сбрасывается к старым значениям

## Файлы:
- `bots_modules/sync_and_cache.py` - исправлена функция `load_bots_state()`

## Дата исправления:
2025-10-16 23:40
