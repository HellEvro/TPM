# 🚀 АКТИВАЦИЯ AI МОДУЛЯ - ПОШАГОВАЯ ИНСТРУКЦИЯ

**Дата:** 2025-10-17  
**Статус:** Готово к активации

---

## ✅ ШАГ 1: Включить режим разработки

Откройте **PowerShell** и выполните:

```powershell
$env:AI_DEV_MODE = "1"
```

Или добавьте в переменные окружения системы постоянно.

**Проверка:**
```powershell
echo $env:AI_DEV_MODE
# Должно вывести: 1
```

---

## ✅ ШАГ 2: Включить AI в конфигурации

Откройте файл: **`bot_engine/bot_config.py`**

Найдите секцию `AIConfig` (строка ~221) и измените:

```python
class AIConfig:
    # БЫЛО:
    AI_ENABLED = False
    AI_ANOMALY_DETECTION_ENABLED = False
    
    # ДОЛЖНО БЫТЬ:
    AI_ENABLED = True
    AI_ANOMALY_DETECTION_ENABLED = True
```

**Сохраните файл!**

---

## ✅ ШАГ 3: Запустить бота

```powershell
python bots.py
```

---

## ✅ ШАГ 4: Проверить логи

В консоли должны появиться сообщения:

```
[AI] 🔧 Режим разработки активирован
[AI_Premium] ✅ Premium модули доступны (режим разработки)
[AI] 🎫 Лицензия: developer
[AI] 📅 Действительна до: 9999-12-31
[AI] ✅ Anomaly Detector загружен
[AI] 🎉 Загружено модулей: 1/4
```

**Если видите эти сообщения - AI активирован успешно!** ✅

---

## 📊 ЧТО МОНИТОРИТЬ

### **1. Обнаружение аномалий**

Ищите в логах строки типа:

```
[EXIT_SCAM] BTC: ❌ БЛОКИРОВКА (AI): Обнаружена аномалия PUMP (severity: 85.3%)
[EXIT_SCAM] ETH: ⚠️ ПРЕДУПРЕЖДЕНИЕ (AI): Аномалия DUMP (severity: 65.0%)
```

### **2. Счетчики блокировок**

За первые 24 часа посчитайте:
- Сколько раз AI заблокировал вход
- Какие типы аномалий (PUMP/DUMP/MANIPULATION)
- Какие монеты чаще блокируются

### **3. Ложные срабатывания**

Если AI блокирует слишком много хороших входов:
- Увеличьте `AI_ANOMALY_BLOCK_THRESHOLD` с 0.7 до 0.8 или 0.9

Если AI пропускает явные pump/dump:
- Уменьшите `AI_ANOMALY_BLOCK_THRESHOLD` с 0.7 до 0.6 или 0.5

---

## 🔧 НАСТРОЙКА THRESHOLD (если нужно)

После мониторинга, если нужно скорректировать:

### **Слишком строгий (много блокировок):**

```python
# bot_engine/bot_config.py
AI_ANOMALY_BLOCK_THRESHOLD = 0.8  # Было 0.7
```

### **Слишком мягкий (пропускает аномалии):**

```python
# bot_engine/bot_config.py
AI_ANOMALY_BLOCK_THRESHOLD = 0.6  # Было 0.7
```

**После изменений:**
```bash
# Если включен Hot Reload - изменения применятся автоматически
# Иначе перезапустите бота:
python bots.py
```

---

## 📈 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

### **Первые 24 часа:**

- ⚠️ **Может быть 5-15 блокировок** (в зависимости от рынка)
- ✅ **Защита от pump/dump схем**
- ✅ **Меньше убыточных входов**

### **Через неделю:**

- 📊 **Соберется статистика** для анализа
- 🎯 **Можно оптимизировать threshold**
- ✅ **Оценить реальную эффективность**

---

## 🎯 КРИТЕРИИ УСПЕХА

AI работает хорошо если:

1. ✅ **Блокирует явные pump/dump** (проверяйте вручную)
2. ✅ **Не блокирует нормальные входы** (<10% ложных срабатываний)
3. ✅ **Улучшает win rate** (меньше убыточных сделок)

---

## 🐛 ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ

### **AI не запускается:**

1. Проверьте `AI_DEV_MODE`:
   ```powershell
   echo $env:AI_DEV_MODE
   ```

2. Проверьте конфиг:
   ```python
   # bot_engine/bot_config.py
   AI_ENABLED = True
   AI_ANOMALY_DETECTION_ENABLED = True
   ```

3. Проверьте зависимости:
   ```bash
   pip list | findstr scikit-learn
   ```

### **Слишком много ошибок в логах:**

Отправьте логи для анализа:
```bash
grep "AI" logs/bots.log
grep "EXIT_SCAM.*AI" logs/bots.log
```

---

## 📞 ПОДДЕРЖКА

Если нужна помощь:
1. Проверьте `docs/AI_USAGE_GUIDE.md`
2. Запустите тест: `python scripts/ai/test_anomaly_detector.py`
3. Проверьте статус: См. код в руководстве

---

**ГОТОВО! ЗАПУСКАЙТЕ!** 🚀

