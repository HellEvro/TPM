# 🤖 AI Модуль для торговой системы

Модульная система искусственного интеллекта, которая обучается на истории трейдов, параметрах стратегии и исторических данных, делает бэктест стратегий и управляет ботами.

## 📋 Возможности

✅ **Обучение на истории трейдов** - анализирует закрытые сделки и их результаты  
✅ **Обучение на параметрах стратегии** - определяет оптимальные параметры RSI, стоп-лоссов и т.д.  
✅ **Обучение на исторических данных** - использует свечи и индикаторы для обучения  
✅ **Бэктест стратегий** - тестирует стратегии на исторических данных  
✅ **Оптимизация стратегий** - находит лучшие параметры на основе анализа  
✅ **Мониторинг данных** - собирает данные из bots.py (свечи, RSI, стохастик, сигналы)  
✅ **Управление ботами** - управляет ботами через API на основе предсказаний  

## 🚀 Быстрый старт

### 1. Установка зависимостей

Все зависимости уже включены в `requirements.txt`. Установите их:

```bash
pip install -r requirements.txt
```

Основные зависимости для AI:
- `scikit-learn>=1.3.0` - машинное обучение
- `numpy>=1.26.0` - математические операции
- `pandas>=2.1.1` - обработка данных
- `joblib>=1.3.0` - сохранение моделей
- `requests>=2.31.0` - API вызовы

### 2. Запуск AI системы

#### Вариант 1: Отдельный процесс

```bash
python ai.py
```

#### Вариант 2: Интеграция в ваш код

```python
from ai import get_ai_system

# Инициализация
ai_system = get_ai_system()

# Запуск
ai_system.start()

# Получение статуса
status = ai_system.get_status()
print(status)
```

### 3. Использование предсказаний

```python
from ai import get_ai_system

ai_system = get_ai_system()

# Предсказание сигнала для монеты
prediction = ai_system.predict_signal('BTCUSDT', {
    'rsi': 30,
    'trend': 'UP',
    'price': 50000
})

print(prediction)
# {
#     'signal': 'LONG',
#     'confidence': 0.75,
#     'predicted_profit': 5.2,
#     'rsi': 30,
#     'trend': 'UP'
# }
```

## 📁 Структура модулей

```
ai.py                          # Главный модуль AI системы
ai_modules/
├── __init__.py
├── ai_data_collector.py      # Сбор данных из bots.py и bot_history.py
├── ai_trainer.py             # Обучение моделей на истории
├── ai_backtester.py          # Бэктест стратегий
├── ai_strategy_optimizer.py  # Оптимизация параметров стратегии
└── ai_bot_manager.py         # Управление ботами через API
```

## 🔧 Конфигурация

Конфигурация находится в `ai.py`:

```python
AI_CONFIG = {
    'bots_service_url': 'http://127.0.0.1:5001',  # URL сервиса bots.py
    'app_service_url': 'http://127.0.0.1:5000',   # URL сервиса app.py
    'data_collection_interval': 60,               # Интервал сбора данных (сек)
    'training_interval': 3600,                    # Интервал обучения (сек)
    'backtest_interval': 86400,                   # Интервал бэктеста (сек)
    'enabled': True,                              # Включить AI систему
    'auto_trading': False,                        # Автоматическая торговля
    'min_trades_for_training': 50                # Минимум сделок для обучения
}
```

## 📊 Данные

Все данные сохраняются в `data/ai/`:

```
data/ai/
├── market_data.json          # Рыночные данные (свечи, индикаторы)
├── bots_data.json            # Данные ботов (статусы, RSI)
├── history_data.json         # История трейдов
├── models/                   # Обученные модели
│   ├── signal_predictor.pkl
│   ├── profit_predictor.pkl
│   └── scaler.pkl
├── backtest_results/         # Результаты бэктеста
└── optimization_results/     # Результаты оптимизации
```

## 🔌 Интеграция с системой

AI система работает совместно с:

1. **bots.py** (порт 5001)
   - Получает данные о ботах через API
   - Получает RSI данные для монет
   - Управляет ботами через API

2. **app.py** (порт 5000)
   - Получает свечи через API
   - Получает рыночные данные

3. **bot_engine/bot_history.py**
   - Использует историю трейдов для обучения
   - Анализирует результаты торговли

## 📈 Как работает обучение

### 1. Обучение на истории трейдов

Система анализирует закрытые сделки и определяет:
- Какие условия входа приводят к прибыли
- Какие условия выхода оптимальны
- Зависимость между RSI, трендом и результатом

### 2. Обучение на параметрах стратегии

Анализирует эффективность разных параметров:
- Оптимальные значения RSI для входа/выхода
- Эффективность стоп-лоссов и тейк-профитов
- Влияние тренда на результаты

### 3. Бэктест стратегий

Тестирует стратегии на исторических данных:
- Симулирует торговлю с разными параметрами
- Сравнивает результаты
- Определяет лучшую стратегию

## 🎯 Примеры использования

### Бэктест стратегии

```python
from ai_modules.ai_backtester import AIBacktester

backtester = AIBacktester()

# Бэктест с конкретными параметрами
results = backtester.backtest_strategy({
    'rsi_long_entry': 29,
    'rsi_long_exit': 65,
    'rsi_short_entry': 71,
    'rsi_short_exit': 35,
    'stop_loss_pct': 2.0,
    'take_profit_pct': 20.0
}, period_days=30)

print(f"Return: {results['total_return']:.2f}%")
print(f"Win Rate: {results['win_rate']:.2f}%")
```

### Оптимизация стратегии

```python
from ai_modules.ai_strategy_optimizer import AIStrategyOptimizer

optimizer = AIStrategyOptimizer()

# Оптимизация параметров
optimized_params = optimizer.optimize_strategy()

print(optimized_params)
# {
#     'rsi_long_entry': 25,
#     'rsi_long_exit': 60,
#     ...
# }
```

### Управление ботами

```python
from ai_modules.ai_bot_manager import AIBotManager

bot_manager = AIBotManager()

# Получить список ботов
bots = bot_manager.get_bots_list()

# Запустить бота
bot_manager.start_bot('BTCUSDT', {
    'rsi_long_entry': 29,
    'rsi_long_exit': 65
})

# Обновить конфигурацию
bot_manager.update_bot_config('BTCUSDT', {
    'rsi_long_entry': 25,
    'rsi_long_exit': 60
})
```

## ⚙️ Настройка

### Минимальные требования для обучения

- Минимум 50 закрытых сделок для обучения моделей
- Данные должны содержать RSI при входе и выходе
- Рекомендуется минимум 100 сделок для качественного обучения

### Интервалы работы

- **Сбор данных**: каждые 60 секунд
- **Обучение**: каждые 3600 секунд (1 час)
- **Бэктест**: каждые 86400 секунд (24 часа)

## 🔍 Мониторинг

Логи сохраняются в `logs/ai.log`:

```bash
tail -f logs/ai.log
```

## 📝 Примечания

1. AI система работает в фоновом режиме после запуска
2. Модели автоматически сохраняются после обучения
3. Данные собираются автоматически из bots.py и bot_history.py
4. Для качественного обучения нужно минимум 50 сделок
5. Бэктест использует исторические данные за последние 30 дней по умолчанию

## 🐛 Устранение неполадок

### Ошибка: "Models not trained"
- Нужно дождаться накопления данных (минимум 50 сделок)
- Запустить обучение вручную через `ai_system.trainer.train_on_history()`

### Ошибка: "Сервис bots.py недоступен"
- Убедитесь что bots.py запущен на порту 5001
- Проверьте URL в конфигурации

### Недостаточно данных для обучения
- Дождитесь накопления истории трейдов
- Минимум 50 закрытых сделок необходимо

## 📚 Дополнительная документация

- `ai_modules/README.md` - подробная документация по модулям
- `docs/HOW_AI_LEARNING_WORKS.md` - как работает обучение
- `docs/AI_STOP_ANALYSIS_PLAN.md` - анализ стоп-лоссов

